# 재생성 작업 지시서 — 접지 기하

아래 표는 `npx tsx tools/kairo-gate.ts --json` 실측에서 **생성**한 것이다 (2026-08-22,
**수리 되돌리기 뒤**). 손으로 적지 않았다 — 그림이 바뀌면 다시 생성할 것.

## ⚠ 2026-08-22 — 수리본 26종을 되돌렸다 (사용자 화면 회귀 보고)

같은 날 앞선 작업이 `python3 tools/repair-kairo-footprint.py --all-failing --adopt` 로
55종을 한꺼번에 수선해서 접지 위반을 **56 → 0** 으로 만들었다. 그런데 사용자가 **실제
화면에서** 회귀를 보고했다:

> "평상연립이나 탁구대같은경우 각도는 제대로인데 **꺼져있다**"
> "**그늘막은 완전히 틀어졌다**"

전/후 확대 대조(`shots/footprint-repair-audit/`)로 재보니 맞는 지적이었다. **접지 게이트는
기하만 보고 사람은 그림을 본다** — 게이트 0 은 그림이 좋아졌다는 뜻이 아니었다.

### 무엇이 그림을 망쳤나

수리 도구의 세 분기 중 **마스크를 단색으로 채우는 분기**다. 스타일 계약은
*"every object carries a baked 1-pixel dark warm outline"* 인데, 이 분기는 `material_colour()`
가 고른 **한 색**으로 정본 다이아몬드를 메우고 **아래 테두리 한 줄에만** 아웃라인을 긋는다.
새로 생긴 실루엣의 **옆·윗 가장자리에는 아웃라인이 없다** (실측 34~100%, 중앙 61%).

1. **다리 사이·들린 바닥 밑의 빈 공간이 메워진다** — 탁구대·평상연립·전망대·정자·잔교가
   그렇게 죽었다. 다리가 사라지고 물건이 어두운 판에 잠긴 것처럼 읽힌다 (= "꺼져있다")
2. **물건 밖으로 아웃라인 없는 색판이 삐져나온다** — `material_colour()` 가 물건의 재질색을
   고르면(오리보트의 흰색 · 눈썰매의 크림색) 배경 위에 찢어진 종이 같은 조각이 생긴다
3. 그리고 **잘라내기 분기**가 타일 밖으로 뻗은 부속을 지운다 — 캐러밴 바퀴 · 트램펄린 앞
   테두리 · 분수 앞면 석벽 · 매표소 개찰구 (실측 원본의 10~17.5%)

⚠ **좌우 반전 분기는 정당하다** — 화소를 하나도 안 바꾸고 축뒤집힘만 되돌린다
(9종 실측: `footprint-mirror.png` 이 뒤집은 백업과 **바이트 동일**).

### 되돌린 기준

**정본 다이아몬드 넓이 대비 채운 화소 비율**(`채운 화소 ÷ 정본 마스크`)을 1차 선별로 쓰고,
**전/후 4배 확대를 눈으로 보고** 판정했다. 눈으로 나쁜 것은 전부 **10% 이상**이었고
(최대 `fishing` 37.2%), 괜찮은 것은 그 아래였다. ⚠ 다만 10% 를 넘어도 **이미 있던 받침대를
메운 경우**(`rent_sup` 10.4% · 빈 공간 채움 29%)는 멀쩡했다 — **단일 문턱으로 자동 판정하면
안 된다.** 잘라내기(`잘린 화소 ÷ 원본`)도 같은 방식으로 10% 를 선별선으로 썼다.

⚠ **직전 작업의 "색 종수" 분류는 미러링을 재고 있었다.** 수리 도구는 **언제나 두 색만**
더한다 (채움색 + 아웃라인색) — "색 15~35종이 늘었다"로 보인 것은 좌우 반전 때문에 화소가
통째로 어긋나서지 색이 는 게 아니다. 그래서 **미러링과 단색 채움은 서로 독립**이고,
`airbounce`·`caravan`·`ticket` 은 **둘 다** 일어난 종이다 — 이 셋은 백업이 아니라
`footprint-mirror.png`(순수 반전)로 되돌려 **축 수리는 살리고 색판만 버렸다.**

| 되돌림 | id | 채움/마스크 | 잘림/원본 | 눈으로 본 것 |
|---|---|---|---|---|
| 낚시터 | `fishing` | 37.2% | 9.5% | 뗏목 옆면이 평판으로 바뀌고 낚싯대가 잘림 |
| 전망대 | `lookout` | 32.4% | 1.6% | 망루 아래 빈 공간이 통째로 메워져 기둥이 사라짐 |
| 붕어빵 | `bungeoppang` | 27.0% | 4.4% | 검붉은 색판이 왼쪽·앞으로 흘러나옴 |
| 탁구대 | `pingpong` | 26.0% | 1.1% | 다리 사이가 메워짐 — **사용자 보고** |
| 안내소 | `info` | 25.9% | 0.3% | 연녹색 판이 양옆으로 삐져나옴 |
| 세면대 열 | `washbasin_row` | 21.5% | 0.4% | 갈색 판 + 앞 가림막 잘림 |
| 파라솔 | `parasol` | 19.5% | 5.7% | 선베드가 갈색 덩어리에 잠김 |
| 눈썰매장 | `snow_sled` | 18.8% | 0.0% | 아웃라인 없는 거대한 크림색 판이 왼쪽에 붙음 |
| 미니 골프 | `minigolf` | 18.6% | 6.9% | 앞 난간 면이 잘려 선 하나로 남음 |
| 정자 | `pavilion` | 16.8% | 0.9% | 검붉은 판 위에 기둥이 잠김 — **사용자 보고와 같은 형태** |
| 선착장 | `dock` | 16.8% | 4.8% | 말뚝이 사라지고 갯벌에 얹힌 모양 |
| 점프쿠션 | `jump_cushion` | 15.5% | 7.9% | 파란 밑판이 잘리고 납작한 주황 판이 붙음 |
| 평상 연립 | `pyeongsang_row` | 15.0% | 9.3% | 다리가 사라짐 — **사용자 보고** |
| 화장실 | `toilet` | 14.7% | 0.2% | 파란 밑단이 잘리고 크림색 쐐기가 생김 |
| 의무실 | `infirmary` | 12.8% | 0.1% | 크림색 쐐기 — 건물이 가라앉아 보임 |
| 선베드 열 | `sunbed_row` | 10.3% | 11.5% | 다리가 사라지고 흰 조각이 튀어나옴 |
| 족구장 | `footvolley` | 8.7% | 5.4% | 아웃라인 36% 뿐인 얇은 판이 찢어진 종이처럼 붙음 |
| 오리배 선착장 | `rent_duck` | 7.5% | 2.0% | 채움색으로 **오리의 흰색**이 뽑혀 배 밖에 흰 조각 |
| 실내 온수풀 | `pool_warm` | 4.7% | 0.1% | 아웃라인 없는 파란 조각이 양옆 실루엣을 깸 |
| 거북섬 | `turtle_island` | 2.2% | 5.4% | 다이아몬드 절단이 거북 머리·앞발을 자름 |
| 수상 트램폴린 | `trampoline_w` | 0.3% | **17.5%** | 앞쪽 테두리가 통째로 잘림 |
| 분수 | `fountain` | 2.9% | **15.0%** | 앞면 석벽이 잘려 땅에 묻힌 모양 |
| 실내 유수풀 | `pool_lazy` | 2.3% | **10.4%** | 오른쪽 끝 벽이 잘리고 크림색 조각이 튀어나옴 |
| **미러 전용** | | | | |
| 에어바운스 파크 | `airbounce` | 18.7% | 4.8% | 파란 색판에 잠김 — 반전은 살리고 채움만 버림 |
| 카라반 | `caravan` | 3.0% | 4.1% | **바퀴가 잘림** + 흰 조각 — 반전은 살림 |
| 매표소 | `ticket` | 4.5% | 5.7% | **개찰구가 바닥 얼룩으로 뭉개짐** — 반전은 살림 |

**남긴 29종**(수리 유지): `bungalow` `cafe` `chicken` `diving` `dj_booth` `float_deck`
`flowerbed` `footbath` `glamping` `icecream` `karaoke` `locker_row` `maple_walk`
`mongol_tent` `nursing` `pension` `pension_duplex` `playground` `rent_kayak` `rent_pedal`
`rent_sup` `sauna` `shop` `shower_row` `sikhye` `slide_large` `snackbar` `stage_river`
`waterwalk` — 채움이 **받침대·바닥판 안에서** 끝나 물건의 읽힘을 안 바꾼 것들이다.
`cafe`·`pension`·`pension_duplex`·`stage_river`·`nursing`·`icecream` 은 **거의 순수 반전**이다.

### 면제 표(`GEOM_HOLDOUT`)도 같이 재검토했다 — 7종 → 5종

사유가 그림과 다른 것이 **둘** 있었고, 그 면제가 **진짜 결함을 가리고 있었다.**

| id | 적혀 있던 사유 | 그림 실측 | 판정 |
|---|---|---|---|
| `shade_net` | "접지는 기둥 발 넷" | **바닥 널판이 통째로** 깔려 있다 (덮임 **98%**). 기울기 왼변 **0.351** (정본 0.500 — 30% 완만) | **면제 제거.** 접지가 다이아몬드가 아닌 게 아니라 **투영이 틀렸다** (사용자: "완전히 틀어졌다") |
| `photozone` | "접지가 발 둘" | **직육면체 받침대**다. 아랫변 기울기 **−0.089** (거의 수평) | **면제 제거.** 발이 둘이라 변이 없는 게 아니라 받침대가 **정면뷰 사각형**이다 |
| `parasol` | "기둥 하나 + 차양" | 파라솔 **+ 선베드 둘**. 접지 = 기둥 1 + 의자 다리 4 | **유지**(사유만 고침). 꼭짓점 축은 계속 재므로 지금도 위반으로 뜬다 |
| `lifering` | "다리 둘 + 튜브" | 맞다. 오른변은 측정 자체가 불가 | **유지** |
| `vending_in` | "타일보다 작은 상자" | 맞다. **왼변 기울기 정확히 0.500** — 투영은 옳다 | **유지** |
| `vending_out` | 위와 같은 물건 | 맞다. 왼변 0.500 | **유지** |
| `arcade` | "타일보다 작은 상자" | 맞다. 오른변 −0.500 | **유지** |

각 면제에 **무엇이 고쳐지면 되돌리는지**를 `tools/ground-geometry.ts` 의 `why` 에 적었다.

⚠ **교훈: 낮은 덮임은 "접지가 작다"의 증상이기도 하고 "투영이 틀렸다"의 증상이기도 하다.**
어느 쪽인지는 **그림을 봐야만** 갈린다 — 숫자에서 유도해 면제하면 결함을 덮는다.

### 이 상태의 실측

| 항목 | 값 |
|---|---|
| 접지 기하 | 위반 **28** (심각 4 · 축뒤집힘 0) · 통과 47 — **되돌리기 전 0** |
| 정적 게이트 | `node tools/check-ui-surface.mjs` **27/27** |
| 단위 | `npx vitest run` **1258** |
| 브라우저 | `PPAJI_URL=http://localhost:5199 npm run verify:kairo` **305/305** · 이음새 위반 0 |
| 팔레트 밖 색 | 시설 75종 **0** (되돌린 26종도 0) |
| 도트 경계율 | 시설 75종 **51.8%** — 되돌린 26종은 45.5% → **47.2%** (단색 판이 사라져 밀도가 돌아왔다) |
| 아틀라스 | `npm run bake:atlas` 144장 · 2048×494 · 295KB |

전/후 확대 대조: `shots/footprint-repair-audit/<id>.png` (왼쪽 백업 · 오른쪽 수리본, 4배)
면제 재검토 대조: `shots/holdout-review/<id>.png` (오른쪽에 정본 다이아몬드 오버레이, 6배)

⚠ **되돌리기로 접지 위반이 0 → 28 로 돌아왔다. 그게 맞다.** 게이트 4 는 원래 경고이고
(`WARN_GATES`), 그 목록이 곧 이 문서다. **게이트 숫자로 그림을 덮지 말 것.**

---

## 한 명령으로 돌리기 — `tools/regen-facility.ts`

아래 「먼저 읽을 것」의 조각들을 **사람이 56번 손으로 이어 붙일 필요가 없다.**
그 이음매가 `tools/regen-facility.ts` 다.

```bash
npx tsx tools/regen-facility.ts --id cafe                # 한 종
npx tsx tools/regen-facility.ts --severe                 # 지금 심각인 종만
npx tsx tools/regen-facility.ts --all --tries 3          # 지금 위반인 종 전부, 종당 최대 3회
npx tsx tools/regen-facility.ts --id cafe --dry-run      # 프롬프트만 찍고 안 돌린다
npx tsx tools/regen-facility.ts --all --dry-run          # 조립 검사 (요약만)
npx tsx tools/regen-facility.ts --verify-gate            # 판정 복제가 게이트와 같은지
```

종당 루프는 **프롬프트 조립 → 생성 → 후처리(1종 추출) → 게이트 판정 → 채택 또는 리롤**이고,
통과할 때까지 `--tries` 만큼 돈다.

**대상은 손으로 적지 않는다.** 위 표를 읽는 게 아니라 팩을 **그 자리에서 재서** 고른다
(`--all` = 지금 위반인 종, `--severe` = 그중 심각). 그림이 바뀌면 목록도 같이 움직인다.

### 프롬프트에 무엇이 들어가나

| 조각 | 출처 | 왜 |
|---|---|---|
| 스타일 블록 | `docs/asset-prompts.md` §SHARED STYLE BLOCK **축자** | 34장이 축자 동일한 것이 이 문서의 성질이다. 도구에 베끼면 문서를 고쳐도 도구만 옛 계약으로 남는다 |
| 항목 본문 | 그 시설이 속한 시트의 대조표 → 칸 번호 → 본문 (라벨로 교차 확인) | 표와 본문이 어긋난 시트가 생겨도 엉뚱한 항목을 안 뽑는다 |
| 규격 줄 | `guideSpecLine()` (= `make-kairo-guide.ts --table`) **+ 영어 요약** | 한국어 정본을 축자로 넣고 영어를 옆에 붙인다. 번역만 넣으면 규격이 바뀌었을 때 조용히 갈라진다 |
| 크로마 키 | 그 시트의 `Canvas:` 문단 (`#00FF00` / `#FF00FF`) | 초록 시트(S1·S2·S4·L2)는 소재에 빨강이 있어서 초록이다 |
| `--ref` 3장 | 발자국 가이드 + 그 시트가 지정한 스타일 크롭 2장 | 항목별 지정(`governs item 1 ONLY`)도 지킨다 — 무시하면 L10 의 실측 실패 모드가 돌아온다 |
| **실패 모드** | 직전 판정(첫 시도는 지금 팩의 실측) | 아래 |

### 실패 모드를 말해 준다 — 이게 리롤과 다른 점

그냥 다시 뽑으면 같은 실수가 반복된다. 도구는 **직전 시도가 무엇을 틀렸는지**를
프롬프트에 적는다. `cafe` 의 실제 출력:

```
WHAT WENT WRONG LAST TIME — do not repeat it:
  - The previous attempt had the two footprint axes SWAPPED. Its ground bottom vertex
    measured at 0.556 of the sprite width, which is where a 3x2 footprint would put it —
    this object is 2x3, so the vertex belongs at 0.400. Do not mirror the guide. The W axis
    (2 tiles) runs toward the LOWER RIGHT and the D axis (3 tiles) runs toward the LOWER
    LEFT. Follow the attached guide's diamond exactly.
  - The previous attempt's ground silhouette overlapped the contract diamond by only IoU
    0.554 (it must reach 0.839). The base is not the right shape: it must be one clean 80x40
    px diamond with the object standing on it, with nothing sticking out below or beside it.
  - The previous attempt still needed to move sideways to sit on its own tile, but it was
    already touching the edge of its canvas, so it could not be moved at all. …
```

⚠ **축뒤집힘에서 가이드를 뒤집지 않는다.** 위(19행)의 "가이드를 뒤집어 다시 뽑아야 한다"와
갈리는 지점이다. 가이드는 정본 접지 마스크에서 파생되어 **구조적으로 옳고**, 뒤집으면
틀린 발자국을 첨부하게 된다. 그리고 이 문서 §레버 표가 이미 재 놓았다 — **좌우 반전으로는
통과 0종**이다. 그래서 도구는 **옳은 가이드를 붙이고 뒤집힘을 말로 지목한다**
(위 예시의 "which is where a 3x2 footprint would put it").

### 채택 규칙 — 통과한 것을 덮어쓰지 않는다

1. 원본을 **먼저** `assets/generated/kairo-regen/<id>/backup.png` 로 백업한다
2. 후보는 **팩 밖**(작업 폴더)에서 판정한다 — 재려고 팩에 먼저 쓰지 않는다
3. 통과했거나 **엄격하게 나아졌을 때만** 채택한다. 판정 순서는
   통과 여부 → 위반 축 수 → 꼭짓점 오차 → IoU 부족 → 기울기 오차이고, **동점은 안 바꾼다**
4. 쓴 **뒤에** 다시 재서, 그래도 나아지지 않았으면 백업으로 되돌린다
5. `--strict-adopt` 를 주면 **통과만** 채택한다 (개선은 무시)

작업 폴더는 종마다 `prompt-N.txt` · `gen-N.png` · `cand-N.png` · `backup.png` 를 남긴다
(`assets/generated/` 아래라 gitignore 다).

### `image_gen` 이 없으면 첫 시도에서 죽는다

이 저장소의 codex 계정에는 권한이 없다 (실측). 그 상태로 `--all` 을 돌리면 56번 실패
로그가 쌓이므로, 도구는 그 오류 문자열을 알아보고 **즉시 전부 중단**한다 (종료 코드 2):

```
❌ 생성 불가 — 이 세션에서는 더 돌려도 전부 같은 이유로 실패한다:
   codex-gen: the built-in image_gen tool never ran in this codex session — …
   → image_gen 을 제공하는 codex 계정의 세션에서 다시 돌릴 것.
```

### 생성 없이 검증한 것 (2026-08-22)

⚠ 아래 숫자의 **56** 은 그때의 위반 수다 (수리 전). 지금은 **28** 이다 —
이 절은 *파이프가 도는가*를 잰 기록이라 그대로 둔다.

이 머신에서는 그림을 못 뽑으므로, **생성 단계만 빼고** 파이프 전체를 태워 봤다.

| 검사 | 방법 | 결과 |
|---|---|---|
| 프롬프트 조립 | `--all --dry-run` | **56/56 조립 OK** (경고 0). 스타일 블록 축자·가이드 존재·크롭 지정 전부 통과 |
| 판정 복제 | `--verify-gate` (`kairo-gate.ts --json` 대조) | **시설 75종 불일치 0** |
| 파이프 양성 대조군 | `--all --use-existing guide` (임시 팩) | **56/56 채택(통과)** — IoU 0.997~1.000. "가이드대로 그리면 게이트를 통과한다"가 추출 경로까지 포함해 실증됐다 |
| 채택 음성 대조군 | 통과종 `office` 에 다른 발자국의 가이드를 먹임 | **유지** — 파일 md5 불변 |
| 권한 없음 | 실제 `sprite-gen gen` 1회 | 1회 시도 후 **전체 중단**, 종료 코드 2 |

⚠ **못 해 본 것은 생성 자체뿐이다** — 실제 그림이 프롬프트를 얼마나 따르는지, 몇 번
리롤해야 통과하는지는 권한 있는 세션에서만 알 수 있다.

⚠ 크로마 키는 **그림의 모서리에서 읽는다**. 가이드는 시트가 초록을 쓰든 말든 언제나
마젠타라(`make-kairo-guide.ts` 의 `BG`), 시트 크로마로 키하면 `keyed_pixels 0` 이 되어
캔버스 전체가 피사체가 된다 — 그 상태로 파라솔(면제 종)이 **게이트를 통과했다** (실측).

---

## 먼저 읽을 것

- 첨부 가이드: `art-reference/guides/kairo/facility__<id>.png` (75장, 이미 구워져 있다)
- 규격 줄: `npx tsx tools/make-kairo-guide.ts --id <id> --table`
- 프롬프트: `docs/asset-prompts.md` — 시트별 코드블록 + §레퍼런스 절의 가이드 첨부 방법
- 확인: 배치마다 `npx tsx tools/kairo-gate.ts --geom` (통과할 때까지 리롤)

⚠ **`축뒤집힘` 표시가 붙은 것은 그냥 리롤하면 안 된다** — 발자국 두 축이 바뀐 그림이라
⚠ **가이드를 뒤집지 마라** — 가이드는 엔진 정본 마스크에서 파생돼 구조적으로 옳고,
좌우 반전은 실측에서 **통과 0종**이었다(아래 레버 표). 가이드를 그대로 붙이고
**프롬프트로 "지난번엔 두 축이 바뀌었다"를 지목**한다 — `tools/regen-facility.ts` 가 그렇게 한다.

⚠ **`여백 없음`** 은 추출기가 평행이동으로 더 밀고 싶었지만 **캔버스에 빈 자리가 없어**
(그림이 좌우 끝에 닿아 있어) 못 민 것이다. 자르는 것은 무손실이 아니므로 갈 수 있는
만큼만 갔다 — 즉 **이 줄들은 위치가 아니라 그림 자체가 틀렸다**는 가장 강한 신호다.

## 우선순위 A — 심각 (꼭짓점이 가로 8텍셀 이상 이탈 = 타일 폭의 1/4)

| 시설 | id | 발자국 | 꼭짓점 이탈 | IoU (문턱) | 기울기 L/R | 덮임 | 상태 |
|---|---|---|---|---|---|---|---|
| 수상 트램폴린 | `trampoline_w` | 3×3 | 25.0tx | 0.406 (≥0.857) | +0.181/-1.012 | 100% | **수리 불가 — 재생성** |
| 에어바운스 파크 | `airbounce` | 6×5 | 16.5tx | 0.616 (≥0.917) | +0.534/-0.500 | 90% | **미러 전용 — 재생성** |
| 점프쿠션 | `jump_cushion` | 4×3 | 15.0tx | 0.619 (≥0.877) | +0.494/-0.502 | 100% | **수리 불가 — 재생성** |
| 미니 골프 | `minigolf` | 4×2 | 10.0tx | 0.731 (≥0.870) | +0.481/-0.500 | 100% | **수리 불가 — 재생성** |

## 우선순위 B — 나머지

| 시설 | id | 발자국 | 꼭짓점 이탈 | IoU (문턱) | 기울기 L/R | 덮임 | 상태 |
|---|---|---|---|---|---|---|---|
| 붕어빵 | `bungeoppang` | 2×1 | 8.0tx | 0.556 (≥0.769) | +0.818/-0.510 | 77% | **수리 불가 — 재생성** |
| 거북섬 | `turtle_island` | 8×6 | 7.5tx | 0.804 (≥0.935) | +0.492/-0.433 | 100% | **수리 불가 — 재생성** |
| 족구장 | `footvolley` | 4×3 | 7tx | 0.772 (≥0.877) | +0.500/-0.494 | 100% | **수리 불가 — 재생성** |
| 낚시터 | `fishing` | 4×2 | 7.0tx | 0.609 (≥0.870) | +0.386/-0.085 | 90% | **수리 불가 — 재생성** |
| 파라솔 | `parasol` | 1×1 | 5tx | 0.508 (≥0.667) | +0.095/— | 87% | **수리 불가 — 재생성** |
| 실내 유수풀 | `pool_lazy` | 6×3 | 5.0tx | 0.794 (≥0.909) | +0.467/-0.443 | 100% | **수리 불가 — 재생성** |
| 탁구대 | `pingpong` | 2×2 | 4.5tx | 0.634 (≥0.800) | +0.566/-0.665 | 92% | **수리 불가 — 재생성** |
| 선베드 열 | `sunbed_row` | 4×1 | 4.0tx | 0.753 (≥0.872) | +0.398/-0.307 | 100% | **수리 불가 — 재생성** |
| 선착장 | `dock` | 1×1 | 2.5tx | 0.642 (≥0.667) | +0.379/-0.900 | 100% | **수리 불가 — 재생성** |
| 매표소 | `ticket` | 3×2 | 2.0tx | 0.724 (≥0.839) | +0.190/-0.969 | 100% | **미러 전용 — 재생성** |
| 카라반 | `caravan` | 4×2 | 1.5tx | 0.748 (≥0.870) | +0.429/-0.558 | 94% | **미러 전용 — 재생성** |
| 그늘막 | `shade_net` | 2×1 | 1.0tx | 0.734 (≥0.769) | +0.351/-0.545 | 97% | 재생성 |
| 평상 연립 | `pyeongsang_row` | 4×1 | 1.0tx | 0.842 (≥0.872) | +0.402/-0.129 | 100% | **수리 불가 — 재생성** |
| 세면대 열 | `washbasin_row` | 3×1 | 0.5tx | 0.776 (≥0.833) | +0.543/-0.524 | 79% | **수리 불가 — 재생성** |
| 화장실 | `toilet` | 2×2 | 0.5tx | 0.765 (≥0.800) | +0.540/-0.510 | 82% | **수리 불가 — 재생성** |
| 실내 온수풀 | `pool_warm` | 4×4 | 0.5tx | 0.802 (≥0.889) | +0.536/-0.536 | 88% | **수리 불가 — 재생성** |
| 안내소 | `info` | 2×2 | 0.5tx | 0.798 (≥0.800) | +0.539/-0.768 | 89% | **수리 불가 — 재생성** |
| 의무실 | `infirmary` | 2×2 | 0.5tx | 0.795 (≥0.800) | +0.538/-0.530 | 85% | **수리 불가 — 재생성** |
| 전망대 | `lookout` | 2×2 | 0.5tx | 0.541 (≥0.800) | +0.751/-0.171 | 79% | **수리 불가 — 재생성** |
| 포토존 | `photozone` | 2×2 | 0.5tx | 0.520 (≥0.800) | +0.403/-0.089 | 65% | 재생성 |
| 정자 | `pavilion` | 3×3 | 0tx | 0.602 (≥0.857) | +0.825/-0.719 | 80% | **수리 불가 — 재생성** |
| 분수 | `fountain` | 2×2 | 0tx | 0.461 (≥0.800) | +0.464/-0.508 | 82% | **수리 불가 — 재생성** |
| 오리배 선착장 | `rent_duck` | 2×2 | 0tx | 0.731 (≥0.800) | +0.710/-0.452 | 90% | **수리 불가 — 재생성** |
| 눈썰매장 | `snow_sled` | 4×5 | 0tx | 0.635 (≥0.901) | +0.551/-0.553 | 77% | **수리 불가 — 재생성** |

## 합계

- 위반 **28종** / 시설 75종 (심각 4 · 축뒤집힘 0)
- 그중 **23종은 되돌린 것**(백업 복원)이고 **3종은 미러 전용으로 되돌린 것**이다 — 아래 §되돌린 이유
- 통과 47종은 건드리지 말 것: `arcade` `bbq_zone` `bungalow` `cafe` `camp_site` `changing_row` `chicken` `diving` `dj_booth` `firepit_row` `float_deck` `flowerbed` `footbath` `glamping` `ice_fishing` `icecream` `jjimjilbang` `junglegym_w` `karaoke` `lifering` `locker_row` `maple_walk` `massage_row` `mongol_tent` `nursing` `office` `parking` `pension` `pension_duplex` `playground` `pool_kids` `rent_kayak` `rent_pedal` `rent_sup` `sauna` `shop` `shower_row` `sikhye` `slide_large` `slide_small` `slide_tube` `snackbar` `stage_river` `storage` `vending_in` `vending_out` `waterwalk`
- 지면 33장은 **33/33 통과** — 재생성 대상 아니다 (후처리가 지면에만 `diamond_mask()` 로
  기하를 강제한다. 시설의 접지 **모양**은 여전히 그림이 책임진다)

## 4방향(선택) — 창구·탑승·무대 25종

`src/data/kairo-facilities.json` 에 `"facings": 4` 를 넣으면 켜진다 (기본 2).
⚠ 그림 4장(`facility__<id>__d0..d3.png`)이 **다 있어야** 켤 것 — 하나라도 없으면
그 방향이 플레이스홀더로 떨어진다.
⚠ 그리고 **봇(`tools/kairo-sim.ts`)이 아직 방향 2개만 본다** — 첫 시설을 4로 켤 때
봇도 같이 옮겨야 헤드리스가 다른 세계를 재지 않는다 (K36 규칙).

## 왜 후처리로 더 못 고치나 — 레버 셋을 다 재 봤다 (2026-08-22)

재생성이 막힌 세션에서 **그림 없이 고칠 수 있는 길**을 셋 다 측정했다. 결론: **9종을
고쳤고 그것이 상한이다.** 다음 사람이 같은 시도를 반복하지 않도록 숫자를 남긴다.

| 레버 | 무손실인가 | 결과 |
|---|---|---|
| **좌우 반전** (축뒤집힘을 되돌린다) | ✅ 무손실 | **통과 0종.** 나아짐 4 · 나빠짐 5 |
| **정수 평행이동** (꼭짓점을 계약 자리로) | ✅ 무손실 | **통과 9종** · 나아짐 30 · 나빠짐 0 — **적용했다** |
| **접지 폭에 맞춰 확대** | ❌ 리샘플 | **51종이 비정수 배율**(1.2~1.6×) — 쓸 수 없다 |
| **④ 정본 마스크를 단색으로 채움** (`repair-kairo-footprint.py`) | ❌ 그림을 바꾼다 | **위반 56 → 0 을 만들었지만 26종을 되돌렸다** — 위 §2026-08-22 |

**① 반전이 안 되는 이유**: `pool_lazy` 는 반전하면 꼭짓점 오차가 **50.5 → 2.5텍셀** 로
거의 완벽해지는데 **IoU 가 여전히 미달**이다. 그림이 **놓인 자리만 틀린 게 아니라 실루엣
자체가 다이아몬드와 다르다.** 방향을 돌린다고 모양이 생기지 않는다.

**② 평행이동의 상한이 9인 근거**: 게이트 판정을 복제해(56건 전부 일치 확인) 모든 정수 dx 를
**전수 탐색**했다. 자르기를 허용하면 2종이 더 통과하지만 하나는 끝 열이 잘려 최소자승
적합이 바뀌는 **우연**이고 하나는 파라솔 차양을 깎는다.

**③ 확대가 안 되는 이유**: 접지 폭이 계약과 맞는 것이 **24/75** 뿐이고 나머지 **51종은
비정수 배율**이 필요하다 (`vending_in` 1.600 · `photozone` 1.524 · `snow_sled` 1.297 …).
⚠ 이 저장소의 렌더 계약은 **정수 스캔라인 · AA 금지**가 기반이다 (CLAUDE.md — "AA `fill()`
은 1px 이음새와 가짜 아웃라인을 만든다"). 비정수 리샘플은 도트 경계를 뭉개므로 각도를
고치려다 **화면 밀도**를 잃는다.

**④ 단색 채움이 안 되는 이유**: 접지 게이트가 재는 것은 **실루엣 최하단 윤곽**뿐이라,
정본 다이아몬드를 아무 색으로나 메우면 세 축이 전부 만점이 된다 — **게이트를 만족시키는
가장 싼 방법이 그림을 망치는 방법이었다.** 스타일 계약의 *baked 1-pixel outline* 은
게이트가 안 재는 축이고, 그래서 다리 사이를 메운 아웃라인 없는 색판이 **위반 0** 으로
통과했다. 위 §2026-08-22 가 26종의 실측이다.
⚠ 게이트에 아웃라인 축을 더한다고 이게 닫히지는 않는다 — 도구가 아웃라인까지 그려도
"다리를 지우고 판을 깐 그림"인 것은 같다. **접지 모양은 그림이 책임진다.**

→ **남은 28종은 재생성이 유일한 길이다.** 위 표가 그것을 측정으로 닫는다.
