#!/usr/bin/env python3
"""(폐기) 배경 그림 텍스처 생성기 — K38 에서 **쓰지 않기로 했다.**

## 왜 폐기했나

지도 바깥을 그려진 원경(하늘+산)으로 덮어 봤더니 눈이 그 수평선을 곧바로
**"무대 뒤에 세운 벽"**으로 읽었다 (사용자: "생각보다 어색하네").

레퍼런스를 다시 보니 이미 답이 적혀 있었다 (`art-reference/competitor/README.md`,
2026-08-17):

  · Pool Slide Story — 경계를 **아예 안 보여 준다.** 하늘도 배경도 없다
  · Terra Nil — 플레이 영역 밖을 **같은 스케일 지형**으로 덮는다. 원경 그림 없음
  · "배경은 뒤에 세운 벽" → 실제 게임은 그렇게 안 한다

그래서 지도 바깥은 **게임의 지면 스프라이트를 구워서** 채운다
(`KairoScene.bakeSurroundTexture`). 이 파일은 그 판단의 기록으로만 남긴다 —
다시 "배경 그림을 깔자"가 나오면 여기부터 읽을 것.
"""
raise SystemExit("K38 에서 폐기했다 — 지도 바깥은 게임 지형으로 채운다 (파일 머리말 참고)")
