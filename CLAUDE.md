# 빠지 타이쿤

한국 강변 수상레저 "빠지"를 짓고 운영하는 모바일 타이쿤. RollerCoaster Tycoon 의 설계 깊이를
Kairosoft 급 접근성으로, 자유도는 Kairosoft 보다 높게.

**이 프로젝트는 코드와 에셋을 전부 AI 가 만든다.** 여러 세션에 걸쳐 작업하므로,
이 문서가 세션 간의 유일한 기억이다. 아래 결정들을 재유도하거나 뒤집지 말 것.

---

## 아키텍처 불변식 — 절대 깨지 말 것

ESLint 로 강제되고 (`eslint.config.js`), `src/sim/invariants.test.ts` 가
"규칙이 실제로 위반을 잡는지"를 검사한다. 규칙을 끄면 그 테스트가 깨진다. **끄지 말 것.**

### 1. `sim/` 은 렌더러를 모른다

`src/sim/**` 은 `phaser`, `render/`, `ui/`, `assets/`, `save/` 를 import 할 수 없다.
의존 방향은 항상 **바깥 → sim** 이다. sim 은 `toSnapshot()` / `fromSnapshot()` 으로
평문 데이터만 주고받는다.

깨지면: 헤드리스 시뮬 러너가 죽고, 단위 테스트가 불가능해지고, 에셋 교체도 막힌다.

### 2. `sim/` 은 결정론적이다

`Math.random()`, `Date.now()`, `performance.now()`, `new Date()` 금지.
시드 RNG(`src/sim/rng.ts`)와 주입된 tick 만 쓴다.

서브시스템은 `rng.fork(salt)` 로 **독립 스트림**을 갖는다. 손님을 하나 더 뽑아도
날씨 시퀀스가 밀리지 않아야, 밸런싱 실험에서 변수를 하나만 바꿀 수 있다.

깨지면: 헤드리스 밸런싱과 골든 시나리오 회귀 테스트가 전부 무의미해진다.

### 3. 시설·장비·연구는 코드가 아니라 데이터다

`src/data/*.json` 에 정의한다. 시설 추가 = JSON 항목 + 스프라이트 ID.
코드 변경 없음. "시설 30~50개"가 감당 가능해지는 유일한 방법.

---

## 의존성 버전을 올리지 말 것 (이유 있음)

성숙도 기준으로 고정했다. **`npm outdated` 을 보고 "최신으로 올려드릴게요" 하지 말 것.**

| 패키지 | 고정 | 최신이 아닌 이유 |
|---|---|---|
| phaser | **3.90.0** (2025-05) | Phaser 4.0 은 2026-04 출시. Phaser 를 고른 이유 자체가 "문서·예제가 방대해 AI 코드 생성이 안정적"인데, 4.x 는 학습 데이터가 얇아 그 이유를 무너뜨린다 |
| typescript | **~5.9.3** (2025-09) | 7.0.2 는 2026-07 출시·릴리스 1회. 툴체인 정렬이 안 됐다 |
| vite | **^7.3** (2025-06) | 8.x 는 2026-03 |
| vitest | **^3.2** (2025-01) | 4.x 보다 검증됨 |
| eslint | **^9.39** | typescript-eslint 8.x 와 가장 많이 쓰인 조합 |

올릴 거면 이유를 여기 적고 올릴 것.

---

## 구조

```
src/
  sim/        순수 시뮬레이션. Phaser·DOM 비의존. Node 에서 단독 실행됨.
    rng.ts        결정론적 RNG (mulberry32) + fork 로 독립 스트림
    clock.ts      고정 timestep 10Hz. 배속은 tick 수를 곱함(tick 크기가 아님)
    terrain.ts    지형 타입 + 판정 헬퍼
    world.ts      타일맵 (지형만. 시설·손님은 별도 소유)
    worldgen.ts   시드에서 결정론적 맵 생성
    game.ts       시뮬 루트 + 스냅샷
    index.ts      공개 표면 — 바깥은 여기로만 접근
  render/     Phaser 전용
    constants.ts      TILE_SIZE, 줌 범위, LOD 단계
    camera.ts         드래그 팬 + 핀치 줌 (중점 고정 보정 포함)
    terrain-texture.ts 지형 전체를 캔버스 한 장에 구움. 변경 시 영역만 재굽기
    scenes/MainScene.ts
  assets/     에셋 추상화 (§ 아래)
  ui/         DOM/CSS HUD. 캔버스 아님 — 에셋 0장, 선명, 다크모드 공짜
  audio/      슬롯 계약. 지금은 SilentAudio (무음) + cues.ts 유니언 — sim 은 모른다
  save/       버전 + 마이그레이션 + 저장소
  data/       시설·장비·연구 정의 JSON (Phase 1~)
tools/
  sim-runner.ts   헤드리스 시뮬레이터
prototype/
  index.html      Phase 0 이전의 렌더링 타당성 검증본. 참고용, 빌드 대상 아님
```

---

## 에셋: "나중에 픽셀로 일괄 교체"

**게임 코드는 파일을 모른다. 논리적 ID 만 안다** (`'facility/shop'`, `'guest/body:3/up/1'`).

- 지금: `ProceduralProvider` — 부팅 시 코드로 스프라이트를 굽는다
- 나중: `AtlasProvider` — AI 픽셀아트 PNG 아틀라스를 로드한다
- 교체 지점: `src/assets/index.ts` 의 `createAssetProvider()` 한 곳뿐. **게임 코드 0줄 변경.**

`src/assets/manifest.json` 이 **이중 역할**을 한다:
1. 코드에게는 계약 — 어떤 ID 가 존재하고 크기·앵커가 무엇인지
2. AI 에셋 생성에게는 작업 지시서 — 무엇을, 몇 px 로, 어떤 스타일로 (`prompt` 필드)

`source` 필드가 파이프라인을 가른다:
- `'procedural'` — **영구히** 코드가 그린다. 물·물결·항적·그림자·지형. AI 생성 대상 아님
- `'ai'` — 지금은 임시 도형, 나중에 AI 픽셀아트로 교체

이 분리 덕에 AI 로 뽑을 이미지가 수백 장이 아니라 **40~50장**이다. 테스트가 이 상한을 지킨다.

---

## 검증

```bash
npm run verify         # typecheck + lint + test  ← 커밋 전 이걸 돌릴 것
npm run sim -- --seed 42 --guests 200 --ticks 20000   # 헤드리스 시뮬 + 성능
npm run sim -- --determinism                          # 결정론 확인
npm run dev            # 폰에서 보려면 출력된 Network URL 사용
npm run verify:mobile  # v1 씬(?v1=1) 모바일 검증 — 폐기된 씬이라 참고용
npm run verify:kairo   # 카이로 씬 브라우저 250종 + 4방 이음새 QA (dev 서버 필요) ← 기본 씬 게이트
npm run seam           # 4방 이음새 QA 단독 (--selftest 로 음성 대조군)
npm run sim:kairo      # 카이로 헤드리스 밸런싱 (--seeds N --weeks N)
                       #   26주는 --seeds 12 · 52주는 --seeds 24 로 판정한다 (K47-③).
                       #   12시드 52주는 '돈이 없어 건설이 막힌다'가 문턱(주×0.4)에 붙어
                       #   있어 중앙값 자리 하나로 흔들린다 — 현금 5분위는 동일했다
npm run verify:pwa     # 빌드 → preview → 오프라인 부팅까지 확인 (홈 화면 앱)
npm run icons          # 앱 아이콘을 코드로 굽는다 (AI 아트 아님 — 포장이다)
```

`npm run sim` 이 도는 것 자체가 불변식 1 의 실증이다 (Phaser 없이 Node 에서 sim 이 돈다).

### 모바일 검증 — 최우선 목표

이 프로젝트의 1순위는 **폰에서 돌아가는 것**이다. "폰에서 확인해주세요"로 넘기지 말고
`npm run verify:mobile` 로 자동 검증할 것. 아이폰 크기(393×852 @3x)·터치를 흉내낸
Chrome 으로 실제 게임을 띄워서 확인한다:

- 부팅·WebGL·콘솔 에러
- 시설 팔레트, **터치 타깃 44px 이상**, 가로 넘침
- 손가락 드래그로 시설 배치 → 확정
- 손님 유입·이동
- FPS
- **핀치 줌** — 반드시 CDP `Input.dispatchTouchEvent` 로 진짜 멀티터치를 보낼 것.
  합성 `PointerEvent` 는 Phaser 가 무시해서 멀쩡한 코드가 실패로 나온다.

브라우저는 번들 chromium 이 아니라 설치된 Chrome 을 쓴다 (`channel: 'chrome'`).

`selftest.html` 은 실기기용 자가진단 페이지다. 폰으로 열면 그 기기의 WebGL·Canvas API·
성능을 재서 판정을 보여준다. 에뮬레이션으로 못 잡는 실기기 문제는 이걸로 확인한다.

**`page.evaluate` 안에서는 이름 있는 함수를 쓰지 말 것.** tsx(esbuild)가 `__name` 헬퍼를
주입하는데 페이지 쪽엔 없어서 ReferenceError 가 난다. 문자열로 넘기는 편이 안전하다.

### 모바일 호환

`src/compat.ts` 가 부팅 전에 먼저 로드된다. 현재 보정:
- `CanvasRenderingContext2D.roundRect` — Safari 16.4 미만에 없다. 없으면 스프라이트를
  굽다가 TypeError 가 나면서 화면이 통째로 검게 남는다.

빌드 타깃은 `['es2020', 'safari15']` — 구형 iOS 까지 내려간다.

---

## 확정된 게임 설계 (v2)

전체 설계는 **`docs/design.md`** — 작업 전에 반드시 읽을 것.

**⚠️ v1(자유 배치 + 실시간 + 자유 스플라인 코스)은 폐기됐다.** 코드에 그 흔적이 남아 있으니
(`PlacementController` 자유 드래그, `CourseEditor` 탭-점찍기, 실시간 배속 UI) v2 로 대체할 것.
왜 바꿨는지는 design.md §1 에 적혀 있다. 되돌리지 말 것.

**한 줄 정의:** 가평 강변 리조트를 키우는 모바일 경영 시뮬. 여름의 "빠지"가 주력, 사계절 운영.

**핵심 루프(30초):** 한 주 진행 → 결산에서 병목·발견 확인 → 구조물을 키우거나 붙임 → 다시 한 주

**바뀐 것과 이유**

| 바뀐 것 | v1 | 최종 | 이유 |
|---|---|---|---|
| 소재 | 빠지 하나 (11종) | **강변 리조트** (시설 75 + 장비 19 = **94종**) | 11종이면 한 시간이면 볼 게 떨어진다 |
| 시간 | 실시간 + 배속 | **주 단위 + 결산 화면** | 실시간은 폰 2분 세션에 안 맞는다. 한 주=0.6초라 공짜로 가능 |
| 배치 | 빈 땅에 툭 놓기 | **연결식 구조물** (데크 확장·부착·건물 증축) | 결정이 거리뿐이라 얇았다. 연결식은 깊고 **폰에서 오히려 쉽다** |
| 코스 | 자유 스플라인 | **프리셋 6종 + 터닝포인트 핸들** | 조각 조합은 탭 10~15회로 폰에 과했다. 프리셋+핸들은 **탭 5회** |
| 코스 깊이 | — | **장비×프리셋 적합도 (19×6)** | 프리셋만으론 30분이면 소진 |
| 수상 제약 | 부력 하중 | **수면 사용 허가 면적**(등급제) | 하중은 계산해보니 안 물었다. 허가는 돈으로 못 산다 |
| 콤보 | 28종 | **73종 3티어** (소40/중22/대11) | 시설 94종에 28종이면 발동이 너무 드물다 |
| 루프 유지 | — | **주간 의사결정 카드 27종** | 없으면 "한 주 진행"이 스킵 버튼이 된다 |
| 시작 | 빈 땅 | **작은 빠지를 물려받음** | 시그니처를 25분→**5분**에 만나게 |

⚠ 위 표의 종수는 **`src/data/*.json` 실측**이다 (2026-08-19). 설계 당시 수치(시설 62·콤보
70·카드 24)는 그 뒤로 늘었다 — 숫자를 인용할 일이 있으면 데이터에서 다시 셀 것.

**v4 에서 다시 바뀐 것 — 외부 적대적 평가 반영**

v3 는 "잘 설계된 경영 시뮬"이 됐지만 **"내 빠지를 만드는 재미"가 뒤로 밀렸다**는 지적을 받았다.

| 바뀐 것 | v3 | v4 | 이유 |
|---|---|---|---|
| 주간 진행 | 0.6초 계산 → 결산 | **계산 0.6초 + 압축 연출 3~5초** | "0.6초에 계산된다"→"0.6초만 보여준다"는 논리 비약이었다. 손님이 노는 광경이 이 게임 최대의 보상인데 리플레이로 격리시켰다 |
| 결산 | 숫자 표 | **혼잡 히트맵 + 요일 막대 + 숫자** | 연출을 스킵하면 다시 엑셀 게임이 된다 |
| 경관 | 콤보 조건 일부 | **리조트 5지표 중 하나로 승격** (만족·경관·스릴·안전·청결) | 모든 시설이 `효율 +10%`로 환원돼 공간 창작 동기가 없었다 |
| 콤보 | 무제한 중복 | **중형 30→15→5%, 대형 리조트당 1회** | 안 그러면 최적 콤보 도배가 정답이 된다 |
| 이벤트 | 선택 카드 | **+ 의뢰 16종 (조건 충족형)** | "조건 미달"이 곧 다음 목표가 된다 |
| 날씨 | 방문객 배수 | **수요 구성 변화** (비→카페·사우나↑) | 배수만이면 RNG 세금이다 |
| 사고 | 순수 확률 | **위험도 4단계 상시 표시** | 안전도 78인데 RNG로 폐쇄면 억울하다. 실패는 "내 선택 때문"이어야 |
| 사계절 | 전부 구현 | **MVP는 여름 완성, 비수기 4주씩 압축** | 여름이 재미없으면 사계절도 소용없다 |
| 재플레이 | 시드 생성 | **맵 타입 3종 + 시나리오 6종** | 맵만 다르면 최적 빌드가 같아 2회차 이유가 없다 |
| 최대 축소 | LOD 한 단계 | **감상 화면 + 리조트 이름 + 공유** | 10시간 뒤 리조트가 스크린샷 찍고 싶은 화면이어야 |
| 수익모델 | Phase F로 미룸 | **프리미엄 유료. 광고·IAP 없음** | 광고/IAP는 세션 구조와 진행 속도를 바꾼다. 미루면 루프를 다시 짜야 한다 |

**게임플레이 동사는 6개로 고정** (짓는다·잇는다·코스를 그린다·의뢰를 받는다·값을 매긴다·사람을 쓴다).
재고관리·식자재·직원 스킬트리·대출·세금·물류는 **넣지 않는다.**

**적대적 검증(design.md 전반에 ⚠ 표시)으로 v3 에서 11개, v4 에서 11개를 고쳤다. 되돌리지 말 것.**

**유지되는 것**
- **손님은 개체 단위 에이전트** (통계 아님). 1,200명 = 0.6ms/frame 실측
- **길찾기는 flow field** — 시설별 거리장 1회 계산, 손님당 O(1)
- **플레이어는 지형을 깎지 않는다.** 주어진 지형을 활용 (지형 보너스)
- **평판의 기반은 퇴장 만족도.** "현재 평균 만족도"는 함정 — 할 게 없는 빠지는 손님이 빨리 나가
  새 손님으로 교체되어 평균이 오히려 높다. Phase 1 에서 실제로 걸렸던 부분

## 페이즈

- **Phase 0 뼈대** ✅ — 셋업, 불변식 강제, sim 코어, 에셋 레이어, 세이브, 렌더러, 헤드리스 러너
- **Phase 1 놓고 보는 맛** ✅ — 시설 데이터, 배치·점유 격자, flow field, 손님 에이전트,
  스프라이트 렌더링·LOD, 모바일 배치 UI. `verify:mobile` 전 항목 통과
- **(구 Phase 2 코스)** — sim 은 완성됐으나 **자유 스플라인 방식은 폐기**

### 카이로 전환 (2026-08-18 K0~K9 완료)

전환 기록은 **`docs/kairo-phases.md`** — 작업 전에 읽을 것. 스펙은
`docs/superpowers/specs/2026-08-17-kairo-asset-contract-design.md` (v2, 적대적 리뷰 반영본),
적대적 리뷰 결과는 `docs/kairo-spec-review-2026-08-17.md`.

- **K0 하네스·계약** ✅ · **K1 투영·카메라** ✅ · **K2 지면·경로** ✅ · **K3 벽·도달검사** ✅
- **K4 시설·슬롯** ✅ · **K5 손님** ✅ · **K6 아쿠아파크** ✅ · **K7 주 루프·결산** ✅
- **K8 콤보·해금·의뢰** ✅ · **K9 밸런싱·골든** ✅ · **K10~K24** ✅
- **K25 벽을 경계로 · 격자 64×48** ✅ · **K26 건물 검토 ①②③** ✅ · **K27 바닥이 곧 방** ✅
- **K28 HUD 재구성 (세로·가로 반응형)** ✅ · **K29 표면 이식 (질감·모션·말풍선)** ✅
- **K30 물려받은 빠지 (맵별 시작 배치)** ✅ · **K31 건물 확장 루프** ✅
- **K32-A 고스트·블록 붓·코스 탭·토지 표시** ✅ · **K33 코스 편집을 화면에서** ✅
- **K34 패널 6종 표면 통일 · 게이트를 TS 까지** ✅ · **K35 밝은 팔레트 + 대비 검사** ✅
- **K36-A 격자 96×72 · 공원 밖 도시 띠 · 입구를 가운데로** ✅ · **K36-B① 출입구를 놓는다** ✅
- **K36-B②③ 입장료·매표소 경유 · 버스 하차 · 배경** ✅
- **K37 버그 4건 (패널 배타 · 깊이 띠 · 손님 보간 깊이 · 코스 겹침) + 높이 표현** ✅
- **K38 지도 바깥을 땅으로 · 윗변 수평 · 실물 산 배경** ✅
- **K39 흐르는 낮** ✅ · **K40 초반** ✅ · **K41 해금 = 사건** ✅ · **K42 심사 + 이동** ✅ ·
  **K43 소원 · 발견 · 입고** ✅ · **K44 걸음 수정 + 크림 스킨** ✅ — 스펙
  (`docs/superpowers/specs/2026-08-19-kairo-live-unlock-design.md` v1.2) 전 단계 완료.
  계획·슬롯 계약은 `docs/plan-live-unlock.md`
- **K45 코스 앵커(선착장) · 보트 연출 · 시설 회전 · 붓 누출 수정** ✅
- **K46 2단 헤더 (레퍼런스 구조) + 질감 2차 (트레이·채움 선택·현금 알약)** ✅ —
  ③ 목표 접기·겹침 수정 · ④ 헤더 합체(한 패널 2줄)·코스 확정 피드백·선착장 1×1 ·
  ⑤ 이동체 깊이 (버스·보트도 손님처럼 **걸친 두 칸 중 가까운 쪽**)
- **K47-① 뉴스 티커 + 알림함 (채널 3분리)** ✅ · **K47-② HUD 재배치 · 스킵 제거** ✅ ·
  **K47-③ 카이로식 조준 배치 (팬으로 정렬) + 위험도 노출** ✅ ·
  **K47-④ 감사 후속 (공허 통과·공원 밖 손님·잠자던 대조군)** ✅ —
  계획 정본 `docs/plan-k47-hud-aiming.md`
- **S1~S4 수영 구역** ✅ — 야외 수영장(pool_water 붓) + 플로팅덱 수영 구역.
  스펙 `docs/superpowers/specs/2026-08-19-swim-zones-design.md` (v1.1 적대 검증 반영)
- **P1 배치·성장의 깊이** ✅ — **P1-A** zone 콤보 면적 비례 · **P1.5** 시설 개선 분기화.
  근거는 `docs/research/pss-gap-proposal.md` (PSS 갭 분석), 기록은 `docs/kairo-phases.md` §P1.
  새 시스템을 안 늘리고 **기존 축에 결정 하나씩**을 얹는 묶음이다

검증: 단위 **892** · 브라우저 **250/250** · 정적 **27/27** (대비 20쌍) · 헤드리스 밸런싱
(`npm run sim:kairo`) · 에셋 게이트 (`npm run gate`)

카이로 씬이 **기본**이다 (K13 부터 — `main.ts` 실측). 폐기된 v1 씬은 **`?v1=1`** 로만 열린다.
그래서 `verify:mobile`(v1 대상)은 참고용이고, **기본 씬의 게이트는 `verify:kairo`** 다.
⚠ "카이로는 `?kairo=1` 뒤에 있다"는 K13 이전 문장이다 — 되살리지 말 것.

**카이로에서 절대 되돌리지 말 것**
- 카메라 줌은 **영구히 1**. 확대는 `ScaleManager` 정수 배율. Phaser 카메라 줌은 폰 393px 에서
  worldView 를 98.25px 밀어 반 픽셀을 되살린다
- 지면·시설·벽은 **정수 스캔라인**으로 그린다. AA `fill()` 은 1px 이음새와 가짜 아웃라인을 만든다
- 스티플은 **아웃라인을 구운 뒤에** 뚫는다. 순서를 바꾸면 투과율이 0% 가 된다
- 앵커는 (bbox 가로중심, 최하단 꼭지점 y). 최하단 꼭지점의 **x** 를 쓰면 비정사각에서 24텍셀 밀린다
- 시설은 손님 길을 막는다. **플로팅덱·선착장만** 밟고 지나간다
- 벽은 **타일 경계**에 있다 (K25). 칸을 막는 게 아니라 **이동**을 막는다 (`blocksMove`).
  `-I`·`-J` 경계는 이웃이 소유한다 — 한 경계를 두 곳에 저장하면 반드시 어긋난다
- 플레이어는 벽도 사각형도 그리지 않는다. **실내 바닥을 깔면 그게 방이다** (`indoor.ts`,
  카이로 원문: "Any indoor tile created will make an indoor area"). 벽은 그 외곽선이고,
  이어 깐 덩어리 하나에 문도 하나다. 사각형 건물로 되돌리면 문제 여섯 개가 같이 돌아온다
- 실내 여부를 **따로 저장하지 말 것.** `terrain.isIndoor` 가 정본이다 — 별도 저장소는
  지형과 어긋난다
- 벽은 **손님보다 낮다** (10텍셀 < 손님 24). 예전 계약은 거꾸로("시설이 벽보다 낮아야")였고,
  그게 곧 "벽이 시설을 가린다"였다. 벽을 유리로 만든 이유도 그 가림이었다
- 바닥은 **사는 것**이다 (`kairo-ground.json` 의 `cost`). 공짜 붓이면 게임이 아니라 에디터다
- **색은 `style.css` 가 소유한다** — TS 에 하드코딩 색 0 (`check-ui-surface.mjs` 가 지킨다).
  규칙은 "인라인 금지"가 아니다: 진행바 폭·캔버스 크기 같은 **데이터**는 인라인으로 남는다.
  캔버스는 `src/ui/tokens.ts` 의 `cssVar()` 로 토큰을 읽는다 (K34)
- 패널은 **표면 셋**뿐이다 — 시트(`.ksheet`) · 전면(`.kover`) · 떠 있는 것
  (`.kover.scrim/.dialog/.frame`). 속은 한 벌(`.kitem.wide`·`.krow`·`.kstat`·`.kchips`).
  새 패널을 만들 때 버튼 클래스를 새로 만들지 말 것 — `.kbtn`/`.on`/`.primary` 로 충분했다
- **"선택됨"은 채움이다** (K46, 레퍼런스 실측): 메인 탭·버튼 `.on` = 노랑 채움
  (`--select-fill`) + 진갈 글씨, 2차 필터 칩만 파랑 채움(`--select-sub`) + 흰 글씨.
  외곽선 강조로 되돌리지 말 것 — 선택으로 안 읽혔다. 층마다 색이 다른 것은 레퍼런스의
  위계 표현이지 "고름이 둘"이 아니다. 파랑 실측값(#396bc2)은 흰 글씨 4.5:1 미달이라
  보정했다 (대비 20쌍 검사가 지킨다)
- 팔레트는 **밝은 크림**이다 (K44, 컨셉 실측: 카드 `#fbe7c3`·바 `#f8d9a2`·윤곽 `#8c5e2b`).
  K35 의 "밝은 표면 + 진한 글씨" 원칙은 그대로고 색상만 파랑→크림으로 옮겼다.
  재질 그림자는 **따뜻한 갈색**이어야 한다 — 남색 그림자는 크림 위에서 물때가 된다
  (K35 규칙의 색상 반전). 주버튼은 가죽 브라운 — 컨셉의 노랑은 크림 위에서 안 읽힌다
- 손님 보간은 **tick 박자를 알아야 한다** (K44, `tickSeconds`). 10Hz 고정 가정으로
  되돌리면 흐르는 낮에서 손님이 스톱모션이 된다. 박자를 바꾸면 `syncTickPace` 와
  하네스의 밀도 환산 상수를 같이 옮길 것. 하루 = 24초 (사용자 체감으로 확정)
- 글씨 양각은 **둘**이다. `--sk-emboss`(밝은 표면의 진한 글씨)와
  `--sk-emboss-on-solid`(진한 칠 위의 흰 글씨 — 위험도 칩·주버튼·토스트·감상 띠).
  하나로 합치면 한쪽이 반드시 뭉갠다
- 모달 스크림(`--scrim-deep`)만은 **어둡다.** 밝은 스크림 위 밝은 상자는 안 떠 보인다
- 대비비 검사가 **20쌍**을 지킨다 (본문 4.5:1 · 보조 3:1 — `check-ui-surface.mjs` 의
  `PAIRS`, K44 에 14쌍 → K46 에 채움 선택·현금 알약 3쌍 → K47-① 에 붓 라벨·티커 띠 1쌍
  → K47-② 에 위험도 칩 면 2쌍).
  레퍼런스의 파랑 `#1b76ff` 를 그대로 쓰면 흰 글씨가 2.8:1 이라 못 쓴다 — 실측으로 걸렸다.
  ⚠ **같은 색을 표면마다 다시 재라** — `--accent` 는 패널 위(4.12)만 재고 바 위는 아무도
  안 쟀는데 크림 바에서 **2.87:1** 로 보조 기준 미달이었다 (K47-① 실측 → `#8f5007`)
- 패널의 `visible` 은 **`!root.hidden`** 을 읽는다. 인라인 `display` 를 읽으면 표면을
  클래스로 옮기는 순간 조용히 거짓이 된다 (`pickForTest` 가 아무것도 안 고르게 된다)
- **UI 는 `style.css` 가 소유한다.** 인라인 스타일로 새 HUD 를 만들지 말 것 — 토큰과
  컴포넌트가 이미 있는데 카이로 씬만 인라인으로 다시 만들어서 투박해졌다 (K28)
- HUD 는 **헤더 하나(2줄) + 뉴스 티커 + 하단 바 하나**다 (K47). 헤더는 레퍼런스처럼
  **한 패널 안의 두 줄**이다 — `.ktop` 이 표면이고 `.khdr-row` 가 줄, 줄 사이는
  구분선(`--tray-divider`)뿐이다. 항목마다 캡슐을 띄우면 풍선 무더기가 된다
  (실측, 사용자 지적). 헤더는 전폭 밴드가 아니라 **내용 폭**이다 (가로 41%→27%)
- **읽는 것은 위, 누르는 것은 아래** (K47-②). 폰 상단은 엄지 사각지대다 —
  헤더에는 버튼이 **0개**이고 지표만 산다(위험도 포함). 상시 컨트롤은 **2개**
  (메뉴·건설), 나머지는 시트 안이다. 화면 예산은 세로 ≤24% · 가로 ≤36%.
  ⚠ K46 의 22/30 으로는 **못 돌아간다** — 전폭 26px 뉴스 띠의 몫(세로 +3.1%p ·
  가로 +6.6%p)이 영구다. 그리고 천장을 정하는 것은 티커가 아니라 **의뢰 칩 기둥**이다
  (최대 4장일 때 +1.8%p). 더 내리려면 칩 기둥을 손대야 하고 그건 K40 계약이다
- **사건 채널은 셋이고 겹치지 않는다** (K47-①): 모달 = **축하**(시간 멈춤 — 해금·
  승급/탈락·새 도구·인물 등장·숨은 콤보·보상 시설) · 티커 = **뉴스**(비차단 — 내가 안
  했는데 일어난 일) · 토스트 = **내 행동의 대답**(거절 이유·확정 −N만). 한 사건을 두
  채널에 넣지 말 것. 근거는 PSS 부정 리뷰 1위가 팝업 과다이고 처방이 "모달 대신
  알림함 적재"였다 (`docs/research/pss-hermes-research.md` §3.3)
- 티커 띠는 **`div` + `role="button"`** 이다. `<button>` 이면 하네스가
  `button,select,input` 을 세는 검사(상시 컨트롤·터치 타깃 44px) **4건이 확정 실패**한다
  — 26px 띠이기 때문이다. 알림함 시트의 루트 필드 이름은 **`root`** 여야 한다
  (`check-ui-surface.mjs` 가 `this.root.hidden` 리터럴로 패널 등록을 검사한다 —
  다른 이름이면 조용히 우회된다)
- 알림함 항목은 **저장하지 않는다** (K47-①, 세션 전용 최근 50건). 뉴스는 상태가 아니라
  흐름이고, 세이브에 넣으면 마이그레이션 짐이 된다. 단 **누적 손님 카운터**는 마일스톤
  판정에 필요하므로 세이브의 **optional 필드**로 둔다 (`visitorsTotal` — optional 이면
  마이그레이션이 필요 없다). 알림함을 **열면** 시간이 멈추는 것은 "시트 = 정지" 규칙과
  일관된 의도된 동작이다 — 티커 띠 자체만 비차단이다
- `prefers-reduced-motion` 가드를 **별도 블록에 쓰지 말 것** (K47-① 실측). 정적 검사가
  첫 블록만 읽어서, 가드가 있는데도 26/27 빨간불이었다 — 기존 가드 블록 **안**에 넣는다
- 방향은 **한 레이아웃**으로 낸다. 미디어 쿼리로 두 벌 만들면 안 본 쪽이 깨진다
- 디버그 오버레이는 `?debug=1` 뒤. 단 `#kairo-debug` 를 **DOM 에서 지우지 말 것** —
  두 하네스가 이 요소로 부팅을 판정한다
- **투영은 카이로와 이미 같다** — 2:1 다이메트릭을 자기상관으로 양쪽 다 실측했다
  (`|dx/dy| = 2.00`). 각도·타일 크기·확대를 다시 건드리지 말 것
- HUD 표면은 **토큰만** 쓴다 (`--sk-*`/`--panel-*`/`--risk-*`). `tools/check-ui-surface.mjs`
  가 하드코딩 hex 0 과 레시피 존재를 지킨다 — 대조군으로 잡히는 걸 확인했다
- 전환은 **`transform`/`opacity` 만**. 레이아웃 속성을 애니메이트하면 프레임이 떨어진다
- `prefers-reduced-motion` 가드를 빼지 말 것
- 새 판은 **빈 땅이 아니다** (`startkit.ts`). 실내동을 안 주면 위생 시설 9종이 전부
  `needs-indoor` 로 막히는데 첫 의뢰가 "기본 위생 3개"다. 봇도 **같은 함수**를 쓴다
- 실내동은 **비워서** 준다 — "화장실을 어디 둘 것인가"는 설계가 정한 첫 결정이다
- 시설은 **문 앞을 막을 수 없고**(`blocks-door`) **실내를 조각낼 수 없다**(`would-strand`).
  벽에만 있던 밀폐 차단이 시설에는 없어서 방이 조용히 죽었다 (K30)
- 손님 검사는 **새 판에서** 돌린다. 앞선 검사들의 잔해 위에서 재면 원인을 알 수 없다
- **건물을 넓히는 것이 카이로의 핵심 루프다.** 건설 시트의 `건물` 탭이 그 입구다 —
  바닥 탭에 섞으면 아무도 못 찾는다 (직접 플레이하다 막혔다)
- 시작 방은 **처음 세 개(화장실·샤워실·락커)가 연달아 들어갈** 크기여야 한다.
  5×3 은 화장실 하나에 다음이 막혔다 (실측 9종 → 4종)
- 못 놓는 이유는 **시트에서 미리** 보여준다 (`자리 없음 · 건물을 넓히세요`).
  거절 메시지는 방법까지 말한다
- 씬에 넘기는 상태(`setLand` 등)는 **씬이 기억했다가 적용**하게 한다. 부르는 쪽 순서에
  기대면 `create()` 전에 불려 조용히 죽는다 — 토지 표시가 K25~K31 내내 그랬다
- 배치는 **조준**이다 (K47-③, 카이로 방식): **픽 → 고스트가 뜸 → 지도를 팬해서 정렬 →
  확정.** 손가락이 고스트를 가리지 않는 것이 원작이 이 방식을 쓰는 이유다. 즉시 배치로
  되돌리지 말 것 — 회전·"장비 탄 손님"이 들어올 자리라 `setGhost` 의 `facing` 인자와
  확정 바의 `↻` 가 그 자리다. 탭은 남아 있지만 **조준을 그 칸으로 옮길 뿐 놓지 않는다**
  (하네스의 `tapTile` 호환 다리). 조준을 쓰는 붓은 시설·건물 블록·바닥·**철거**·이동
  2단계이고, 출입구·코스만 탭이다 (`aimingBrush`)
  · ⚠ **순진한 "화면 중앙 고정"은 판의 32% 를 배치 불가로 만든다.** 카메라 클램프가
    뷰를 `worldBounds` 안에 가둬서 화면 중앙이 갈 수 있는 범위가 제한된다 (5등급 토지
    커버 68% — 남쪽 `i+j` 삼각형과 동서 끝이 영원히 중앙에 못 온다). 그래서 `aim` 이
    **정본**이고 레티클은 화면 표시일 뿐이다. 팬은 레티클 칸의 **증분**만 고스트에
    더하므로 클램프에 닿으면 고스트가 중앙에서 밀려나며 가장자리까지 따라간다 —
    최대 오프셋 < 반뷰라 **100% 커버가 수학적으로 보장**된다
  · ⚠ 클램프를 배치 모드에서 **푸는 것은 금지** — `BACKDROP 48` 은 "하늘이 어디서도
    안 보인다" 검사의 근거이고 굽기 여백은 `SURROUND_PAD 128` 뿐이다. PAD 를 키우면
    구운 캔버스가 19MB → 32MB (폰에서 위험)
  · ⚠ 그 **음성 대조군은 5등급 토지에서만 성립한다.** 1등급 땅(26×48)은 최대 `i+j` 가
    클램프 상한보다 작아 어느 끝도 클램프에 안 걸린다 (실측 4/4 도달) — 작은 판에는
    커버 구멍 자체가 없으므로 중앙 고정으로 되돌려도 대조군이 안 잡힌다
  · 십자 화살표는 **안 넣는다** — 확정 바 한 줄(377px)에 44px 4개가 안 들어가고,
    오프셋 방식이 커버를 보장하므로 기능적으로 불필요하다. 미세 조정은 팬 8~16텍셀이면 한 칸
  · 판정은 **칸이 바뀐 프레임에만** 한다. 실측 야외 0.054ms · 실내 0.325ms · 물 위
    0.240ms 라 폰 3~5배로 잡으면 최악 1.6ms — 매 프레임이면 위험하고 칸이 바뀔 때만이면 무료
- **화면에서 잰 값을 쓰는 코드는 "그 화면이 최종 상태인지"를 같이 물어야 한다** (K47-③).
  `startAim()` 이 조준 자리를 **확정 바가 뜨기 전에** 정했다 — 그 시점엔 라벨이 빈 div 라
  바가 83px(라벨이 들면 99px)이고 `confirmInset` 이 16px 작아 레티클 y 가 8텍셀, 칸으로
  **(+1,+1)** 밀렸다. `(+1,+1)` 은 `i−j` 를 안 바꾸고 `i+j` 만 2 바꾸므로 "가로는 맞는데
  세로만 절반"이라는 기묘한 증상이 됐다 (커서 누적은 처음부터 멀쩡했다 — **출발 칸**이
  틀렸다). 고친 방법은 `refreshAim()` 이 바를 띄운 뒤 **한 번 더 재는 것**이다 (두 번이면
  수렴). ⚠ 팬 중에는 절대 재유도하지 말 것 — 오프셋 설계가 무너져 가장자리 32% 가 다시 막힌다
- **조준 중에는 시간이 안 흐른다** (K47-③). 확정 바는 `PanelHost` 패널이 아니라
  `flowTick` 이 안 멈췄고, 그래서 **확정 바를 띄운 채 주가 마감 → 결산 위로 확정 클릭 →
  옛 현금 기준 지출**이 가능했다. `hud.confirming` 이면 tick 을 쉰다 (`skipForward` 도 같은
  규칙). ⚠ 확정 바를 `PanelHost` 에 **등록하지는 말 것** — 스크림 없는 바라 배타 규칙이
  시트·결산과 충돌한다. 대신 시트를 열 때 `hideConfirm()` 으로 취소시킨다
- 바닥도 **토지 제한을 받는다**. 시설만 막고 바닥을 열어 두면 안 산 땅에 건물이 선다
- 길찾기의 `canCross` 는 `build()` 와 `next()` **둘 다** 봐야 한다. 하나만 넣으면
  거리장은 맞는데 손님이 벽을 통과한다
- 실내 시설(9종)은 **벽에 접함이 아니라 실내 바닥 위**다 (`requiresIndoor`). 경계는 두 칸이
  공유하므로 `hasAnyEdge` 로 보면 방 **바깥**에도 붙는다 (K26 ①)
- "손님이 설 수 있는 칸"의 정의는 **`guestWalkable` 하나**다. 손님·문 자리·도달 검사가
  전부 이걸 써야 한다. 갈라지면 시설로 막힌 칸에 문이 뚫린다 (K26 ②)
- 실내 도달 검사는 **설 수 없는 칸을 건너뛴다.** 시설이 놓인 칸은 막힌 게 아니라 쓰이는
  중이다 — 안 그러면 시설이 든 방을 못 넓힌다
- 문은 **양쪽 다** 설 수 있어야 한다. 바깥칸만 보면 문이 시설로 꽉 찬 칸으로 열린다
- `WallGrid` 배열은 **(w+1)×(h+1)** 이다. w×h 면 격자 가장자리 건물의 바깥쪽 벽이
  저장될 자리가 없어 조용히 사라진다 (K26 ③)
- 토지 해금(`landW`/`landH`)은 **봇에도 넣는다**. 안 넣으면 헤드리스 숫자와 실제 판이 갈라진다
- 만족도에는 하향 압력(걷기·대기)이 있어야 한다. 없으면 전 판 만점이 되어 배치가 무의미해진다
- 한 손님의 방문은 하루(120tick) 안에 끝나야 한다. 넘으면 공원이 영구 포화된다
- 결산은 **히트맵 → 요일 막대 → 숫자** 순서. 숫자만이면 다시 엑셀 게임이다
- 위험도는 **상시 표시** (K47-② 부터 헤더 2줄째). 사고는 경계·위험 단계에서만, 주당 5% 이하
- 위험도 비율에는 **노출**이 곱해진다 (K47-③): `ratio = (risk/denom) × min(1, crowd/40)`.
  ⚠ 곱하는 자리가 `riskPoints` 가 아니라 **비율**인 것이 핵심이다 — 비율 `risk/(risk+safety)`
  는 **배율 불변**이라 점수에 곱하면 `safety = 0` 일 때 여전히 1.0 이 되어 아무 일도 안
  일어난다. 이것 때문에 새 판이 첫 화면부터 빨간 `위험` 이었다 (시작 킷 탁구대가 위험 2,
  안전 시설 0 → `2/(2+0) = 1.0` 포화. 손님이 0명인데 사고 확률이 최대였다).
  `riskPoints`/`safetyPoints` 는 **원값 그대로** 보고한다 — "무엇을 지었나"와 "지금 누가
  쓰나"는 다른 질문이다. 처방(`safetyNeeded`)도 노출을 봐야 한다: 안 넣으면 손님이 절반인
  판에서 "구명함 4개"라 해 놓고 2개만 지어도 단계가 내려가 처방이 틀린다
- **문은 놓을 수 있다** (K36-B). 건물은 막다른 곳이 아니라 **통로**가 될 수 있다 —
  `blocksMove` 는 `EDGE_SOLID` 만 막으므로 길찾기는 손댈 것이 없다. 단:
  · `DoorSet` 은 **희망**이지 상태가 아니다 (벽은 여전히 실내 바닥에서 파생)
  · 정규형(`+I`/`+J`)으로만 담는다 — 두 형태로 담으면 반드시 어긋난다
  · **자동 문을 없애지 말 것** — 희망이 없는 방이 조용히 죽는다. 첫 플레이어 문이
    자동 문을 대신한다
  · 플레이어 문은 `reach` 를 요구하면 **안 된다.** 통과용 문은 아직 안 닿는 쪽으로
    나는 것이 목적이다 — 요구하면 영원히 못 고른다
  · 후보 판정은 `doorCandidates` 하나를 UI 와 sim 이 공유한다
- 지형 플래그는 **셋**이다 (K36): `walkable`(육지인가) · `guestWalk`(손님이 지나가나) ·
  `buildable`(플레이어가 지을 수 있나). 도시 띠(도로·보도·가로수)는 걸을 수는 있어도
  못 짓는다. 막는 곳은 셋 — `placement.check` · 바닥 붓 · `paintFloor`. 하나만 막으면
  플레이어가 도로를 덮는다
- 격자 크기는 **`KairoTerrain.WIDTH/HEIGHT`** 가 정본이다 (K36 에 render 에서 옮겼다).
  세이브 마이그레이션이 알아야 하는데 `save/` 가 `render/` 를 import 할 수 없다
- 공원 위 **8줄은 도시 띠**다 (`CITY_BAND`). 가로수·차도2·정류장·가로수4 —
  정류장에서 입구까지 다섯 칸을 걷는다. 붙여 놓으면 "밖에서 안으로"가 안 읽힌다
- 입구는 **맵 가로 한가운데**이고 토지는 거기서 **좌우로** 자란다 (`landRectAt`).
  `(0,0)` 사각형으로 되돌리면 한쪽으로만 자라 "좌우로 넓혀 간다"가 안 된다
- 격자를 바꾸면 **세이브 마이그레이션이 필수**다. `fromSnapshot` 셋이 저장된 크기를
  그대로 쓰므로, 안 하면 새 영역이 "잔디처럼 보이지만 영원히 죽은 칸"이 된다
- **봇과 골든은 실제 격자로 돈다** (K36). K9~K35 동안 40×32 였고, 그래서 헤드리스
  밸런싱이 실제 판과 다른 세계를 쟀다. 격자를 바꾸면 이 둘도 같이 옮길 것
- **손님은 포장한 바닥만 지나간다** (K32-B). 지형의 `walkable`(육지인가)과
  `guestWalk`(손님이 지나가나)는 다르다 — 잔디에는 **지을 수는** 있다. 길을 까는 것이
  동선 설계이고, **문은 포장이 닿은 쪽에만 난다** (길이 곧 입구다). 봇에도 `ensurePath`
  를 넣어야 한다 — 안 넣으면 헤드리스가 "아무것도 못 짓는 판"이 된다
- **화면이 되는지는 진짜 터치로 본다.** 코스 편집이 "핸들은 화면에서 직접 끈다"고 적어 두고
  실제로는 핸들이 화면 밖(x=−284)에 있었는데, 검사가 `moveHandleForTest` 로 좌표를 직접
  넣어서 5건이 통과하고 있었다 (K33). 백도어는 sim 검사에만 쓴다
- 편집 화면을 열면 **카메라가 대상을 잡는다** (`frameCourse`). 가려진 높이는 재서 쓴다 —
  상수로 박으면 바가 두 줄이 되는 순간 어긋난다
- 코스가 뻗는 방향은 **잔교가 뻗은 쪽**이다 (`dockCandidates` 의 `dir`). `{x:0,y:1}` 하드코딩은
  물이 +j 인 맵 하나만 가정한 값이었다
- 도달 검사는 전부 **손님 판정**(`guestWalkable`)으로 잰다. 지형만 보면 "검사는 통과하는데
  손님은 못 온다"가 생긴다
- **입장료·시설 요금은 1/10 눈금**이다 (K36-B②). 설계서의 ₩8,000 은 데이터에 `fee: 800`.
  설계 금액을 그대로 넣으면 한 항목이 열 배가 된다 — 새 수입원은 **기존 중앙값과 비율을
  먼저 재고** 넣을 것. 입장료 ₩10,000 을 명목가대로 넣었더니 26주 현금이 400만 → 2,854만
- 매표소는 **1등급 해금**이다. 그것이 입구라서, 2등급에 두면 새 판이 아무도 못 들어온다
- 버스 시간표는 **회전을 바꾸고 총량은 안 바꾼다**. 정차 중에만 태우되 하루 끝에 남은 수요를
  그날 안에 털어 낸다 — 안 그러면 시간표가 조용히 수요를 깎아 밸런스가 왜 움직였는지 못 본다
  (`admission.test.ts` 의 보존 검사가 지킨다: 게이트 588 = 상시 588)
- 버스 시계는 **주 루프 tick 하나**다 (`bus.setElapsed(tick)`). 러너가 `t` 를 따로 세면
  압축 연출에서 손님만 걷고 버스는 붙박인다 — 연출은 재생 프레임의 tick 으로 `busStateAt`
  을 다시 부른다. `onFrame` 은 **연출 중에 버스를 손대지 않는다** (매 프레임 덮어쓰기라 이긴다)
- 주 시작 판정은 **`rng.fork`** 로 굴린다. 사고 판정이 공유 rng 에서 뽑아서, 사고 유무가
  뽑기 3회만큼 **날씨 시퀀스를 통째로 밀고** 있었다 (K36-B③에서 발견). 그래서 "사고만 다른
  두 판"을 만들 수 없었고, 대조 테스트가 K9 부터 다른 것을 재고 있었다
- 칸 안의 깊이는 **띠 상수**다 (`iso.ts` 의 `Z_*`, K37). 순서는
  지면 0 → 뒤벽 1 → 시설 2 → **앞벽 3** → 손님 4 → 얼굴 5 → 이모트 6 → 고스트 7.
  앞벽과 시설이 둘 다 `+2` 로 **동률**이었고, Phaser 는 동률이면 삽입 순서라 시설이
  늘 이겨서 실내 시설이 벽 밖으로 삐져나왔다. 매직 넘버로 되돌리지 말 것.
  · **"벽은 손님보다 낮다"는 그대로다** (손님 4 > 앞벽 3) — 뒤집으면 유리 벽의 이유가 사라진다
  · 전부 4096 미만이어야 한다 — 넘으면 다음 칸을 침범한다
- 손님 깊이는 **두 칸 중 가까운 쪽**이다 (`spanDepthKey`). 위치는 보간하는데 깊이를
  목적 칸으로 주면, 위로(= `i+j` 감소) 걸을 때 출발 칸 지면에 파묻힌다. 압축 연출도
  같은 규칙이라 `PlaybackFrame` 이 `fromI`/`fromJ` 를 담는다 (재생 전용 — 세이브엔 안 넣는다)
- 코스는 **겹칠 수 없다** (K37). `validateCourse(… , others)` 가 `dock-taken`(같은 잔교)과
  `overlap`(샘플 3칸 이내)을 본다. 없을 때 코스 3개가 잔교·핸들까지 **완전히 같은 자리**에
  쌓였다. 기본 제안(`suggestCourse`)은 **빈 잔교부터** 고르고, 지도에서 탭한 잔교는 고정한다
  (안 고정하면 제안이 탭을 덮어써 "탭했는데 안 옮겨진다"가 된다)
- 처방은 **가능한 것**을 말한다. 잔교가 하나뿐이면 "다른 잔교를 고르세요"가 막다른 길이라
  "선착장을 더 지으세요"로 갈린다 — 새 판은 시작 킷이 유일한 잔교를 쓰므로 언제나 이 상태다
- 패널은 **한 번에 하나**다 (`src/ui/panels.ts` 의 `PanelHost`, K37). 8종이 각자 `hidden` 을
  만지고 "다른 걸 닫는다"를 아는 곳이 없었다. 등록을 안 하면 **배타가 기본**이다 —
  잊으면 겹치는 쪽이 기본이면 이 버그가 다시 생긴다. 예외 둘:
  · 감상(`.kover.frame`)은 `exclusive: false` — 지도가 보여야 감상이고, 다른 패널을
    `display:none` 으로 감췄다 되돌리므로 `hide()` 로 닫으면 복원이 안 된다
  · 주간 카드는 `modal: true` — 선택 전에 밀려나면 주가 조용히 넘어간다
- 카드의 "살 수 있나"는 **확정 지출**로 잰다 (`optionCertainCash`). `chance` 가 붙은 선택지는
  확률적 벌금이지 지금 내는 돈이 아니다. `optionCash` 로 재면 `safety_check`·`typhoon` 은
  선택지가 둘 다 돈이 들어서 현금이 마르면 **아무것도 못 고르고**, 카드가 모달이라 메뉴도
  안 열려 판이 잠긴다 (대조군 테스트가 그 상태를 고정해 둔다)
- 하네스 절은 **자기가 연 것을 닫고** 넘어간다. 카드 절이 카드를 열어 둔 채 넘어가서
  모달을 넣은 순간 뒤따르는 "새 판" 5건이 전부 실패했다. 잔해 위에서 재면 원인을 알 수 없다
- 높이는 **맵이 갖고 태어난다** (K37). 플레이어는 지형을 깎지 않는다 (설계 불변식).
  `terrain.levels` (0~3) 가 정본이고 **물은 영구히 0** 이다 — 사용자가 명시적으로 뺐다
  ("물쪽은 굳이 높낮이 할필요없어"). `paint` 가 물을 칠하면 단을 내리고 `setLevel` 은 물을 거부한다
- 한 단 = **8텍셀** (`LEVEL_H`). 칸 위의 **모든 것**이 `lift()` 를 탄다 — 지면·벽·문 표식·
  시설·고스트·손님·버스·재생 프레임. 하나만 빠뜨리면 그것만 파묻힌다 (브라우저 검사가
  종류별로 잰다. 대조군: 시설만 빼면 24px 오차로 잡힌다)
- 절벽 치마는 **윗면과 한 장으로 굽는다** (`columnTextureId`). 따로 두면 그 칸 안에서
  윗면과 깊이가 동률이 되는데, 그게 정확히 버그 ①의 형태다. 한 장이면 `depthKey` 를
  아예 안 건드린다 — 칸을 "밑면에 앵커된 기둥"으로 그리면 `(i+j, i)` 순서가 그대로 맞다
- 산은 **봉우리에서의 타원 거리**로 만든다. 열까지의 거리로만 정하면 등고선이 세로 직선이
  되어 **계단식 논**처럼 보인다 (실측). 봉우리는 맵 **밖 조금**에 둔다 — 안쪽이면 최고 단이
  공원 절반을 먹는다
- 등고선 흔들림은 **몇 줄마다**(`CONTOUR_RUN`) 한 칸씩. 줄마다 바꾸면 테라스가 톱니가 된다
- **통행은 BFS 로 증명한다.** 0 이어야 하는 칸(가운데·도시 띠·물)을 누른 뒤 그 집합에서
  BFS 거리를 재서 상한으로 쓴다. 1-Lipschitz 함수 둘의 min 은 1-Lipschitz 이므로 단차가
  1 을 넘을 수 없다 — 손으로 맞추면 도시 띠 경계에 3단 절벽이 32곳 생겼다 (실측).
  급경사가 생기면 테라스가 **닿지 않는 죽은 땅**이 된다
- 시설은 **단이 균일한 발자국**에만 (`level-mixed`). 판정 순서는 토지 → 도로 → **경사** →
  지형이다 — 가장 바깥 제약부터 말해야 처방이 맞는다
- 암반(`mountain_rock`)은 **절벽 테두리만** 칠한다. 최고 단 전체를 덮으면 정상 평지가
  회색 덩어리가 된다 (실측 17칸 폭). 잔디와 플래그가 같고(`buildable: true`) —
  짓는 자리를 줄이면 높이가 보상이 아니라 벌점이 된다 — 다만 `paintable: false` 다
- 지면 종류 플래그는 **넷**이다: `walkable`·`guestWalk`·`buildable`·`paintable`.
  넷째가 없어서 암반이 바닥 붓 팔레트에 조용히 새어 들어갔다 (12개 → 13개)
- ⚠ 붓 팔레트의 두 필터는 **`buildable && paintable !== false`** 를 본다. `pool_water`
  는 `buildable:false · paintable:true` 로 **두 축이 갈라진 첫 종류**라 그 필터에 안
  걸리고, `main.ts` 에 2등급 조건이 붙은 **별도 항목**으로 들어간다 (S3). 같은 모양의
  종류(칠할 수는 있는데 지을 수는 없는 것)를 또 넣으면 팔레트에서 조용히 사라진다 —
  `kairo-ground.json` 의 pool_water 주석이 말하는 "paintable ?? buildable" 은 **의도**
  이지 지금 코드가 아니다. 종류를 늘리기 전에 필터부터 통일할 것
- **새 지면 종류는 세 곳에 넣는다**: `kairo-ground.json`(sim) · `kairo-render-contract.json`
  (그림) · `GROUND_BASE`(색). 렌더 계약을 빼먹으면 Phaser 의 "텍스처 없음"(초록 테두리
  검은 사각형)이 화면에 깔린다 — 계약 불일치 검사가 있으니 `npm run verify` 를 돌릴 것
- 이음새 규칙을 **반대로 적용하지 말 것**: 가로수는 유기적일 때 대비가 2.9배로 튀어
  PAVED 로 옮겼지만, 암반은 PAVED 일 때 내부가 균일해 테두리가 2.66배로 튀고 유기적이면
  0건이다. 자연 재질은 결이 있어야 이음선이 묻힌다
- **그려진 원경 배경을 세우지 말 것** (K38). 아이소 다이아몬드는 사각 화면을 못 채워 네
  귀퉁이가 비는데, 거기에 하늘+산 그림을 깔아 봤더니 눈이 곧바로 **"무대 뒤에 세운 벽"**
  으로 읽었다. 레퍼런스가 이미 답을 냈다 (`art-reference/competitor/README.md`, 2026-08-17):
  · **Pool Slide Story** — 경계를 아예 안 보여 준다. 하늘도 배경도 지평선도 없다
  · **Terra Nil** — 플레이 영역 밖을 **같은 스케일 지형**으로 덮는다. 원경 그림 없음
  · 그 문서가 이미 적어 둔 문장: "배경은 뒤에 세운 벽 → 실제 게임은 그렇게 안 한다"
  ⚠ 두 번 어겼다. 새 배경 아이디어가 나오면 **저 README 부터 읽을 것**
- 카이로 방식(경계를 안 보여주기)은 **우리 격자로 불가능하다** (계산해 봤다). 다이아몬드
  안에 화면 사각형을 가두면 카메라 중심이 가로 296·세로 148텍셀 안에서만 움직여 지도의
  37%밖에 못 본다. 카이로는 맵이 화면보다 작아서 되는 것이고 96×72 는 훨씬 크다
- 그래서 **지도 바깥을 게임의 지면 스프라이트로 덮는다** (`bakeSurroundTexture`).
  그림 파일이 아니다 — 다른 붓으로 그리면 경계에서 결이 어긋나 "여기부터는 딴 그림"이 된다
- 바깥은 **가장자리를 이어서** 깐다 (좌표를 격자 안으로 클램프). 한 종류로 깔면 균일한
  빈 들판이 되어 "밖에도 세상이 있다"가 아니라 "아무것도 없다"로 읽힌다. 이으면 강은
  강으로·도로는 도로로 화면 밖까지 나간다 — K36 세이브 마이그레이션과 같은 수법이다
- 바깥은 격자에서 멀어질수록 **산으로 올라간다** — 공원이 골짜기에 앉은 모양이 된다.
  격자 안 타일과 **같은 기둥 함수**(`columnTexture`)를 쓴다: 다른 코드로 그리면 절벽
  모양·색이 갈라지고 그 차이가 곧 경계선으로 보인다. 물은 안 올린다
- 장식 등고선의 흔들림은 **저주파 사인 합**이다. 좌표 해시로 흔들면 칸마다 값이 튀어
  계단이 **격자무늬**로 보인다 (실측). 사인은 이웃끼리 이어져 등고선이 부드럽게 굽는다
- 바깥은 **캔버스 한 장으로 굽는다.** 타일 이미지로 깔면 7,000장이 더 필요한데
  (바깥 넓이 ÷ 격자점 밀도 256) 6,912장으로 도는 판에 두 배는 폰에서 위험하다.
  구운 뒤 갱신은 안 한다 — 장식이라 가장자리가 낡아도 안 보인다
- 굽기는 **평평한 밑칠을 먼저** 깐 뒤 기둥을 올리고, **격자 안쪽도 굽는다.** 올린 칸과
  이웃 사이 1px 틈 뒤가 비면 하늘색이 샌다. 아이소 마름모는 가장자리가 계단이라 반올림이
  어떻게든 남는다 — 기하를 더 맞추는 것보다 **뒤를 막는 쪽**이 확실하다
- **하늘은 어디서도 보이면 안 된다.** 카메라 여백(`BACKDROP_ABOVE/BELOW`)을 48 로 줄이고
  구운 지형에 128 여백을 둘러 고무줄로 끌어도 안 드러나게 했다. 검사가 지도 **네 귀퉁이 +
  중앙**에서 화면 전체를 훑는다 (음성 대조군: 지형을 끄면 11%)
  ⚠ 대조군은 **귀퉁이에서** 재야 한다 — 중앙은 다이아몬드가 화면을 다 덮어서 지형을 꺼도
  0% 다. 중앙에서 재면 아무것도 안 재는 검사가 된다
- 절차적 배경 3겹은 **안전망**이고, 굽기가 성공하면 **아예 만들지 않는다**.
  안 보이는 것과 없는 것은 다르다 — 타일스프라이트 3장이 텍스처 **9.6MB** 를 붙들고
  있었다 (총 31MB 중). 폰이 1순위인 프로젝트에서 안 보이는 배경이 게임 내용의 몇 배를
  쓰면 안 된다. 검사는 "3겹이 있다"가 아니라 **"둘 중 하나가 성립한다"** 를 본다
- 시설 등급 상한은 `FACILITY_MAX_LEVEL`(5)이다. 지형 단 `KairoTerrain.MAX_LEVEL`(3)과
  이름이 같아서, 하나를 고치며 다른 하나를 고쳤다고 착각하기 쉬웠다 (K38 점검에서 지적)
- 지면 종류를 늘리려면 **네 곳**이 맞아야 한다: 시뮬 데이터 · 렌더 계약 · **색** · (필요하면)
  `paintable`. 색이 없으면 `drawGround` 가 **다리 가지로 떨어져 나무 널판**이 그려지는데,
  `npm run verify` 는 그대로 통과하고 `seam` 만 "겹침 2800px"이라는 엉뚱한 이름으로 잡는다
  (K38 점검에서 실측). 지금은 검사 2건이 색 유무를 대조한다
- 지도 바깥은 **격자 밖이라 설치가 구조적으로 불가능**하다. 플래그로 막는 것이 아니라
  sim 이 그 좌표를 모른다. 못 짓는 경계는 도시 띠(도로)이고 K36 부터 그대로다
- 지도 바깥은 **어둡게 하지 않는다** — 도시 띠와 같은 이유다. 영원히 못 사는 땅이지
  아직 못 산 땅이 아니다
- 화면 픽셀로 재는 검사는 **네 가지에 걸린다** (K38 에서 전부 실측으로 밟았다):
  · 카메라를 옮긴 **같은 프레임**을 읽으면 좌표는 새 자리인데 픽셀은 옛 화면이다
  · 단이 있는 칸은 기둥 텍스처라 `tileScreenRect` 기준으로 찍으면 이웃을 읽는다
  · 토지 밖 타일은 tint 되므로 텍스처 원색과 다르다
  · 한 점만 찍으면 1~2px 어긋날 때 이웃 **변형**을 읽는다 — 작은 창으로 훑을 것
- 렌더 검사의 음성 대조군은 **코드로** 둔다 (`setRenderFaultForTest`). 손으로 주입해
  확인한 것은 다음 사람에게 안 남는다 — 아키텍처 점검이 "새 ★ 18개 중 코드에 붙은
  대조군은 1개"라고 짚었다. `seam --selftest` 가 픽셀에 위반을 주입하듯, 씬의 **그리기
  규칙**에 되돌리기를 주입하고 같은 판정으로 잡히는지 본다 (실측: 치마 +1 → 구멍 126곳 ·
  리프트 0 → 오차 24px · 앞벽 띠 동률 → 2 vs 2, 원복 시 0/0/3>2)
- 깊이는 **화면에 올라간 오브젝트에서** 읽는다 (`depthProbeForTest`). 띠 상수끼리 비교하면
  상수 산수라 그리기가 틀려도 통과한다
- 경사 판정은 `terrain.levelUniform` **하나**다. `placement.check` 가 `levelAt` 을 직접
  돌려 같은 규칙이 두 벌이었고, `levelUniform` 은 production 이 안 쓰는 채로 하네스만
  불렀다 — 규칙이 갈라지면 "검사는 통과하는데 게임은 다르게 판정"이 된다
- 검사가 **자기가 쓰는 함수로 시험 자리를 고르면** 자기참조다. 그 함수가 통째로 틀려도
  통과한다 — 경사 검사는 발자국의 단을 **직접 비교**해 자리를 찾는다 (독립 유도)
- 지도 바깥 굽기는 **격자 안쪽을 안 굽는다.** 한때 타일 사이 실틈을 막으려 통째로
  구웠는데, 원인은 실틈이 아니라 컬럼 텍스처 안의 1px 구멍이었고 기하로 고쳤다.
  고친 뒤 재보니 지형 판을 **통째로 꺼도** 산 지역 하늘 픽셀이 0 이다
- **한 시드 대조로 밸런스 주장을 검증하지 말 것.** 시설 하나를 세우면 손님이 재분배되어
  회전이 오히려 오르는 시드가 있다 (12시드 중 2). 합계로 재고, 다수 조건도 같이 볼 것
- **52주 판정은 `--seeds 24` 로** 한다 (K47-③). 26주는 12시드 그대로. 12시드 52주에서만
  `돈이 없어 건설이 막힌다`(중앙 22회/판, 문턱 20.8)가 뜨는데, 현금 5분위가 변경 전후
  **완전히 동일**(85/108/160/208/265만)하고 24시드에서 그 지표 중앙값이 14 → 14 로
  안 움직였다 — 경제가 아니라 **문턱에 붙어 있던 지표가 표본에서 흔들린 것**이다.
  위 "합계로 재고 다수 조건도 같이 볼 것"을 표본 크기에 적용한 결과다
- **개수를 세는 검사는 조용히 죽는다** (K47-② 실측). "감상 화면을 닫으면 HUD 가 돌아온다"가
  버튼 **개수**로 재고 있었는데, 상시 컨트롤이 2가 되자 감상 화면 자신의 버튼 2개와 수가
  같아져 `2 → 2 → 2` 가 됐다 — 수가 우연히 일치하면 "가려졌다"와 "안 가려졌다"를 구분
  못 한다. **정체로 재도록** 고칠 것 (헤더·티커·바의 표시 상태를 직접 본다)
- **시간은 흐르는 낮이다** (K39). `WeekRunner` 는 `begin/step/finish` 이고 `run()` 은 그
  합성 — **항등 검사**(`week-identity.test.ts`)가 빨간불이면 흐름 쪽 작업 금지.
  `run()` 은 배치 전용이라 흐름 중 직접 부르면 던진다 (하네스는 `abort()` 로 치운다.
  production 에 `abort()` 금지 — 반 주가 조용히 증발한다)
- 씬의 시계 주인은 **하나다** (`clockOwner`). 흐름 모드에서 장식용 유휴 시뮬을 되살리면
  헤드리스와 다른 세계가 된다. 날씨 뽑기는 그 날 첫 tick 소비 시점 — 순서를 옮기면 항등이 깨진다
- **결산 → 카드 순서** (K39, UX 검수 §5). 카드는 begin 전에 골라야 modifiers 가 그 주에
  적용된다
- **스킵 버튼은 없다** (K47-②). 시간은 흐르는 낮으로 자동이고(하루 24초) PSS 도 본편에
  스킵이 없다 — 빨리감기는 엔드게임 보상 하나뿐이다. 스킵을 누르고 싶은 순간은 "할 게
  없다"는 신호이므로 스킵으로 가릴 게 아니라 목표 밀도로 고친다. 배속 2× 는 메뉴에
  남고, 첫 심사 통과 보상은 **이동 붓만**이다. `skipForward()` 함수는 **하루 고정으로
  남겨 둔다** — 하네스가 시간을 감는 유일한 수단이다 (세이브의 `weekSkip` 필드는
  제거했다. optional 이라 마이그레이션이 필요 없었고, 정본은 `exam.toolsUnlocked` 다)
- **픽셀 검사는 무채 상태에서** — 황혼 틴트가 지형 픽셀을 하늘색 대역으로 민다 (실측 24%).
  `flow.frozen` + `setDayPhase(null)` 후 잰다. 측정 구간에 rAF 가 tick 을 더 미는 것도 같은 함정
- 연출·소리는 **슬롯 계약으로만** (`docs/plan-live-unlock.md` §1) — FX 는 등록부,
  오디오는 `AudioBus` 큐. 등록부 밖 하드코딩 이펙트/사운드를 만들지 말 것 (Phase G·오디오
  작업이 "목록 채우기"가 되게 하는 장치다).
  · 오디오 슬롯은 **있다**: `src/audio/` 의 `SilentAudio` + `cues.ts` 유니언, 호출부
    (`audio.play('sfx/…')`)가 사건 지점마다 심겨 있다. 남은 일은 `WebAudioBus` 한 줄 교체
  · ⚠ **캔버스 FX 등록부는 아직 없다** (2026-08-19 실측): `src/render/kairo/fx.ts`·
    `playFx`·`FX_REGISTRY` 가 코드에 0건이고, CSS 쪽은 `.fx-pop` 하나뿐이다.
    첫 연출을 넣는 사람이 **등록부부터** 만들 것 — 호출부에 직접 tween 을 쓰면 계약이
    깨진 채로 굳는다
- **시작 킷 시설은 동선 위에** (K40). 막다른 곁길에 두면 왕복 걷기 감점으로 첫 주
  만족이 붕괴한다 (실측 41→5). 초반 밸런스는 `onboarding.test.ts` 가 12조합 **합계**로
  지킨다 — 개별 주는 노이즈 ±20 이라 개별 문턱은 무해한 변경마다 깨진다
- 건설 시트는 **해금분만 + 티저 2장** (K40). 등급 잠김을 목록에 되돌리지 말 것 —
  73장 중 45% 잠김은 소음이었다 (UX 검수 §4). 전 목록 열람은 도감 소관
- 의뢰 칩 기둥은 **div 다** — 버튼으로 바꾸면 "상시 컨트롤 2개" 검사에 걸린다 (실측).
  검사는 `button, select, input` 을 다 센다. 접기 손잡이(머리 행)도 K47-② 부터
  `div + role="button"` 이고, **머리에서 `stopPropagation`** 을 해야 접으면서 메뉴가
  같이 열리지 않는다. 하네스는 머리를 id 가 아니라 **"칩 목록이 아닌 첫 자식"**으로
  잡으므로 머리와 칩 목록 사이에 다른 요소를 끼우지 말 것
- **해금은 `isUnlocked` 하나로 묻는다** (K41) — 골격(등급)과 사건(grant)의 합.
  `requiredGrade` 기본값은 99 다 (안 적으면 공짜였던 구멍). 사건 해금은 세이브 v7 의
  상태다 — 등급에서 재생성할 수 없다. 해금 그래프 검사가 교착을 막는다: **의뢰 조건은
  골격만으로 충족 가능해야 한다** (보상 시설을 조건 재료로 쓰면 언젠가 못 푸는 판이 나온다)
- **승급은 심사로만, 강등은 자동** (K42). 판정에 무작위 금지 — 부분 점수만
  (실패는 내 선택 때문이어야, v4). 조건 평가는 `evaluateCondition` 하나 — 의뢰와
  심사가 갈라지면 "의뢰로는 3개인데 심사로는 2개"가 된다. 봇도 응시한다 —
  안 넣으면 1등급에 갇혀 골든이 성장 없는 세계를 잰다
- **bootKairo 뒤에 `await` 를 넣지 말 것** — main.ts 주석의 경고가 실화가 됐다 (K41).
  동적 import 는 전부 boot 앞에. 첫 프레임이 TDZ 를 건드려 **간헐** 부팅 실패가 된다
- 밸런싱의 "돈이 없다"는 **현금이 예비비 아래**일 때만이다 (K42) — 예비비를 지키는
  신중과 궁핍을 한 통에 세면 소비형 경제가 영구 경보가 된다
- **토지 사각형 비교는 언제나 i0+w 다** (K43). `ni >= land.w` 류의 크기-vs-경계
  비교가 봇의 ensurePath 를 K36 부터 통째로 죽여 놓고 있었다 (BFS 가 게이트에서
  첫 발도 못 뗌) — 밸런싱이 "길을 못 까는 봇"의 세계를 쟀다
- 소원 EXP 는 **유형 집계 파생**이다 (K43) — 1,200 에이전트에 개인사를 붙이지 말 것.
  인물 사슬은 그래프 검사가 지킨다 (비시작 인물은 정확히 한 번 초대 — 하은이
  고아였던 실데이터 버그를 잡았다)
- **수영 구역은 저장하지 않는다** (S1, "바닥이 곧 방"의 물 버전). `swim.ts` 가 지면
  (`pool_water` 덩어리 4칸+)과 덱 밀폐 물에서 매번 파생한다. **자연 연못(덱 0장)은
  구역이 아니다** — 안 그러면 판마다 공짜 구역이 생기고 허가 면적을 먹는다 (실측 2개)
- 강 구역은 **permitArea 의 첫 소비자**다 (S1). 그전까지 permitArea 는 정의만 있고
  소비처 0 인 죽은 값이었다 — 코스 편입은 백로그. 밀폐를 완성하는 배치가 허가를
  넘으면 `permit-over` 로 거절된다
- 구역은 손님의 **가상 시설**이다 (S2, `ZONE_HANDLE_BASE`). 입수는 rideFrom 재사용
  (세이브 v7 그대로), 유영은 tick rng. 나올 때 입수점으로 올라온다 — 물 칸에서
  걸으면 거리장이 없어 갇힌다
- `evaluateCombos` 의 zone 콤보는 **모든 호출부가 같은 zones 를** 받아야 한다 (S4) —
  의뢰(`progress.ts`)·심사(`exam.ts`)·소원(`wishes.ts`)·건설 토스트(`main.ts`)·
  봇(`kairo-sim.ts`). 한쪽만 주면 "의뢰로는 3개, 심사로는 2개"가 된다.
  ⚠ **아직 한 곳이 안 받는다** (2026-08-19 실측): 도감 발견 경로
  `activeComboIds(placement)` (`ui/kairo-catalog.ts` → `main.ts` 의 `noteDiscoveries`)
  가 zones 인자 없이 부른다 — zone 콤보 3종이 **실게임에서 영원히 발견되지 않는다**.
  봇은 zones 를 넘기므로 헤드리스 숫자와 실제 판이 갈라진 상태다
- **콤보는 경제에 물려 있다** (P2-A). `comboEffect` 가 원점수를 **포화 곡선**으로 바꿔
  `WeekOptions.combos` 에 **숫자 둘만** 넘긴다 (카드·직원·코스와 같은 방식 — 규칙은
  `combos.ts` 소유, 계수는 `kairo-combos.json` 의 `economy`).
  · ⚠ **하드 상한(`min`)으로 자르지 말 것** — cap 을 넘는 순간 콤보 전부가 무가치해져
    "죽은 축"이 후반부에 그대로 재생산된다. 포화는 하나 더 붙이면 언제나 조금 더 준다
  · 계수는 **게임의 기존 눈금에 묶여 있다**: `satCap 10` = 등급 문턱 간격(한 등급은
    넘길 수 있고 두 등급은 못 넘긴다 — 검사가 `GRADES` 와 대조한다) · `revCap 18` <
    요금 슬라이더 폭(콤보가 "값을 매긴다"는 동사를 덮으면 안 된다)
  · 매출은 **입장료·코스 매출 제외** (입장료는 `입장객×정가×요금배율` 성질을 지켜야 하고,
    코스는 적합도라는 제 축이 있어 두 번 세면 안 된다)
  · 만족은 **주간 집계**에 더한다 — 손님 개체에 더하면 다음 주까지 남아 두 번 먹는다
  · `main.ts` 와 `tools/kairo-sim.ts` **둘 다** 넘겨야 한다 (정적 검사가 강제한다)
- **zone 콤보는 면적에 비례한다** (P1-A, `zoneAreaScale`). 곡선은
  `clamp(sqrt(area / base), 1, cap)` 이고 `base`·`cap` 은 **데이터**(`kairo-combos.json`,
  불변식 3). 상수 보너스로 되돌리면 "구역을 키운다"가 게임에서 사라진다:
  · **제곱근** — 선형이면 거대 구역 하나가 판을 지배한다 (면적 4배 = 배율 2배)
  · ⚠ **cap 은 필수다.** 수영장은 땅이라 `permitArea` 를 **안 쓴다** — 크기 상한이
    아예 없다. cap 이 없으면 **잔디 전체를 물로 칠하는 것이 정답**이 된다. 강 구역만
    허가가 2차 상한이라 짝이 맞는다 (지금 셋 다 `cap: 2.0`, base 8·8·12)
  · ⚠ **바닥은 1이다.** `base` 미만을 깎지 않는다 — 면적은 **보상 축이지 벌점 축이
    아니다.** 깎으면 이미 지은 판이 조용히 약해지고 최소 규격 4칸 풀이 "짓지 말 걸"이 된다
  · ⚠ 발동은 **면적 큰 순으로 정렬**해서 낸다 (`findZone` 의 `hits.sort`). 안 그러면
    좌상단 **스캔 순서**가 티어 체감의 첫 슬롯(중형 30%)을 먹어, 공들여 키운 큰 구역이
    5% 슬롯으로 떨어진다. 정렬 키는 면적 → 좌표라 결정론적이다
  · 실효 배율은 **티어 체감 × 면적 배율**이다. 둘은 서로를 상쇄하지 않는다 —
    체감은 "몇 개나 깔았나", 면적은 "하나를 얼마나 키웠나"를 잰다
  · 봇의 `growPool` 은 **매주 4칸씩 32칸까지**다 (`POOL_GROW_TILES` 4 · `POOL_TARGET_TILES`
    32 = base × cap²). ⚠ 한 번에 32칸(칸값 4만 × 32 = 128만)을 파게 하면 예비비 위로
    남는 돈이 주당 수십만뿐이라 **오히려 풀이 사라진다** (실측). 4칸만 파면 배율이 영영 1 이라
    헤드리스가 "상수 콤보 세계"를 잰다 — 목표 면적은 cap 도달점에 맞춘다
- **시설 개선은 3단계에서 갈라진다** (P1.5, `FacilitySpecialty`). 회전(정원 +1) ·
  수익(요금 +25%) · 평판(이용 만족 +3) 중 하나, 5단계에서 효과 ×2 (`SPECIALTY_LEVEL` 3 ·
  `SPECIALTY_DOUBLE_LEVEL` 5). 단조 강화("+등급")로 되돌리면 개선에 결정이 없어진다
  · ⚠ **셋은 서로 다른 자원에 꽂는다** — 체류·현금·평판. 합치면 스탯 하나와 같아져
    "무엇을 고를까"가 죽는다 (PSS 아이템 3속성의 요지). 검사가 "수익과 평판이 **반대
    방향**으로 갈린다"로 이 분리를 고정한다
  · 허용 특화는 **데이터가 정한다** (`specialties`, 불변식 3). 75종 중 60종이 셋 다 ·
    `capacity 0` 분위기 시설 14종은 **빈 배열**(0→1 이 되면 손님이 DJ 부스에 줄을 선다) ·
    매표소는 회전만 (게이트라 요금·이용 만족이 안 붙는다)
  · 세이브는 **v7 그대로**다 (`specialty?` optional). ⚠ 대신 `fromSnapshot` 이 **데이터가
    안 허용하는 특화를 불러올 때 지운다** — 세이브가 데이터를 이기면 불변식 3 이 죽는다
  · ⚠ **개선 자체를 막지 않는다.** 안 고르고 5단계까지 갈 수 있고 그 경로는 P1.5 이전과
    완전히 같다 (음성 대조군). 막으면 특화를 모르는 봇이 3단계에 갇혀 밸런싱이 다른
    세계를 잰다 — "봇과 골든은 실제 격자로 돈다"(K36)와 같은 종류의 함정
  · 정원의 정본은 `placement.capacityOf` 다. `def.capacity` 를 직접 읽으면 특화가
    화면에만 뜨고 손님은 원래 인원만 들어간다 (⚠ `week.ts`·`risk.ts` 가 아직 그렇다 —
    다음 할 일 7)
- **슬롯이 늘었는지는 손님 점유표로 잰다** (`slotCountForTest`). `capacityOf` 끼리 비교하면
  **상수 산수**라 손님 쪽이 `def.capacity` 를 그대로 읽어도 통과한다 — K38 의 "깊이는
  화면에 올라간 오브젝트에서 읽는다"와 같은 규칙이다. 점유표는 **늘려만 준다**:
  주 도중에 특화를 고르면 정원만 늘고 슬롯은 옛 길이로 남는데, 줄이면 높은 번호 슬롯을
  쓰던 손님이 영영 해제되지 않는다
- 이동체(버스·견인 보트)의 깊이도 **걸친 두 칸 중 가까운 쪽**이다 (K46-⑤,
  `spanDepthKey(floor, ceil)`). round 한 칸 스냅은 위로 갈 때 이전 칸 지면에 파묻힌다

**다음 할 일** (2026-08-20 P1 뒤 재실측. ✅ **해소**: HUD 버튼 위치·구조 불만 → K47-②
"읽는 것은 위, 누르는 것은 아래" · 카이로식 뉴스 배너 부재 → K47-① 티커 + 알림함 ·
탭 배치의 손가락 가림 → K47-③ 조준 배치 · 공허하게 통과하던 안전요원 검사와 공원 밖
손님을 세던 위험도 → K47-④ · "구역을 키울 이유가 없다"·"개선에 결정이 없다" → P1)

1. **소리가 하나도 안 난다.** `src/` 전체에 `AudioContext`·`new Audio` 가 0건이다.
   ⚠ 단 **슬롯은 이미 있다** — `src/audio/` 의 `SilentAudio` 와 `cues.ts` 유니언,
   그리고 사건 지점마다 심긴 `audio.play('sfx/…')` 호출부. 남은 일은 처음부터 짜는 게
   아니라 `WebAudioBus`(파일 로드 + iOS 제스처 언락 + 음소거 UI)로 **한 줄 교체**다.
   사용자가 마지막으로 미뤄 둔 것이지만, 없는 것 중 **체감이 가장 큰** 항목이다
2. **Phase G — 만든 에셋을 게임이 안 쓴다.** `assets/generated/sprites/` 에 런 94개가
   쌓여 있는데 카이로 씬은 `kairo-procedural` **이미지 129장 전부 플레이스홀더**로 돈다
   (`npm run gate` 실측: 시설 75종 · 스프라이트 명세 101 · 이미지 129장 · 생성물 0장).
   ⚠ `public/assets/atlas.json` 이 있지만 **v1 씬용**이다 (`facility/slide`·`pool`·
   `vehicle/passing-boat` — 카이로 계약 ID 가 아니다). "아틀라스가 이미 있네"로 착각하지 말 것.
   카이로용 아틀라스를 굽고 `HybridProvider` 로 붙이는 것이 남은 일이다
3. **후반에 지을 게 없다 — 그런데 실제로는 후반에 못 간다.** 설계상 막는 것은 자리가
   아니라 **등급 상한**이다 (5등급 `maxGuests` 230 · `permitArea` 500). 그런데 봇 실측은
   거기까지도 못 간다: **심사 등급 중앙값이 52주에도 1** 이다 (12시드, 최대 2). 즉
   "상한을 푸는 축이 없다"보다 **심사 페이싱이 먼저 막고 있다.** 두 항목은 뿌리가 같으니
   같이 볼 것 — 페이싱을 안 고치면 상한을 풀어도 아무도 그 상한을 못 만난다.
   ⚠ P1-A 가 여기에 하나를 더 얹었다: **면적 축도 같은 문턱 뒤에 있다** (아래 4번)
4. **수영 구역이 봇 세계에 거의 안 생긴다.** 수영장 붓이 **2등급 해금**인데 3번 때문에
   중앙값 등급이 1 이라, 12시드 52주 **수영 구역 중앙 0개**(p75 1)다. S2 밸런싱은
   p75 시드로만 재졌다 — 수영 경제가 밸런스에 실려 있다고 말하려면 3번이 먼저다.
   ⚠ P1-A 로 이 항목의 무게가 커졌다: 구역이 없으면 **면적 비례도 잴 수 없다.**
   판 절반이 2등급을 못 달아 수영장 자체가 없으니, 봇이 32칸까지 넓히도록 고쳐도
   그 코드가 도는 시드가 절반뿐이다 — 면적 축의 밸런싱은 심사 페이싱과 한 묶음이다
5. **S4 백로그 — 수영 구역이 반쪽이다** (스펙 §4.6):
   · 의뢰 2종("첫 수영 구역"·"나이트풀")이 아직 없다 — `kairo-quests.json` 은 16종 그대로
     (조건 kind 확장이 필요해서 미뤘다)
   · 구역 첫 밀폐의 **발견 셀레브레이션**이 없다 (스펙 §2.4 의 "해금이 아니라 발견")
   · **안전요원 인접 위험 감면**이 없다 — `swimRiskPoints` 는 구역당 +2 만 센다
   · 도감 발견 경로가 zones 를 안 받아 **zone 콤보 3종이 실게임에서 발견 불가**
     (위 불변식 참조 — 코드 한 줄이지만 밸런스가 아니라 도감 정합 문제다)
6. **콤보 배선의 나머지 둘** (P2-A 로 경제 연결은 끝났다). ① **UI 가 이 숫자를 안
   보여준다** — 결산에 "콤보 +3.1점 / +4.5%" 줄이 없으면 플레이어는 배선이 생긴 줄
   모른다 ② **봇이 콤보를 노리지 않는다** — 지금 발동 증가(20→23)는 시설이 늘어 우연히
   터진 것이라 헤드리스는 이 축의 **하한만** 재고 있다 (상한 +7.9점/+13.2% 는 검사로만 확인)
7. **특화가 아직 두 곳에 안 닿는다** (P1.5 실측). `week.ts` 의 `supply()`(수요별 공급 =
   병목 계산)와 `risk.ts` 의 위험 점수가 `def.capacity` 를 **직접** 읽는다 —
   `placement.capacityOf` 가 아니다. 그래서 **회전 특화가 병목·위험 계산에 반영되지 않는다**
   (손님 슬롯은 늘었는데 결산은 옛 정원으로 잰다). 고칠 때 둘을 같이 옮길 것 — 한쪽만
   바꾸면 "정원은 늘었는데 위험은 그대로"가 되어 어느 쪽이 정본인지 다시 갈라진다
8. **봇이 특화를 안 고르고, 강 구역을 안 만든다.** `tools/kairo-sim.ts` 에
   `chooseSpecialty` 호출이 **0건**이고 (봇은 개선만 올린다), 수영 구역도 `poolZones`
   만 센다 — 플로팅덱으로 물을 밀폐하지 않으니 `riverZones` 는 봇 세계에 **영영 0** 이다.
   즉 헤드리스가 P1 이 만든 **두 축(특화 선택 · 강 구역 면적)을 안 잰다** — "봇과 골든은
   실제 격자로 돈다"(K36)와 같은 종류의 괴리다. 특화는 세 갈래를 시드로 갈라 뽑게 하고,
   강 구역은 덱 배치 규칙이 필요해 더 크다 (별도 작업)
9. **코스가 `permitArea` 를 안 쓴다.** S1 에서 강 구역이 첫 소비자가 됐지만 코스는 여전히
   허가 밖이다 — 수면을 쓰는 두 가지가 다른 회계를 쓰는 상태다. 스펙이 "밸런스 변화가
   섞이면 원인을 못 가린다"고 별도 작업으로 뺐다
10. **화면 밀도** — 상위 5색 43% vs 카이로 15%. 에셋 문제라 2번에 딸려 있다
11. **경사 도로·산 위 수목** — 사용자가 "나중에"로 미뤘다 (K37). 지금은 계단식으로 오르고
    산은 암반·초원뿐이다. 수목은 도시 띠 가로수처럼 **지면 종류**로 넣을 수 있다
12. **버스는 아직 임시 도형** (아이소 상자 하나). 산은 이제 실제 지형이라 이 목록에서 뺐다
13. **지도 바깥은 구운 뒤 갱신 안 한다** — 플레이어가 가장자리 칸을 칠하면 바깥 띠가 옛
    종류로 남는다. 지금은 장식이라 안 보이고, 토지 해금이 맵 가장자리까지 가면 보이기
    시작한다 (`docs/k38-arch-review.md` §고쳐야 할 것 7)
14. **확정 바가 떠 있는 동안 시간이 완전히 멈춘다** (K47-③ 이 의도적으로 넣은 것의 뒷면).
    붓을 든 채 방치하면 게임이 멈춘 것처럼 보이고, 지금 유일한 신호는 **티커의 붓 라벨**
    뿐이다. 옛 버그(옛 현금 기준 지출)로 되돌리지 말고 **신호를 키우거나 유휴 타임아웃**으로
    풀 것 — 정지 자체를 없애면 K47-③ 이 고친 원버그가 돌아온다
15. **티커 띠가 아이콘을 둘 그린다** — 고정 `📰` 와 항목 아이콘이 겹쳐 보인다 (외양).
    ⚠ 채널 계약(§사건 채널)은 안 건드리는, 순수 표시 문제다
16. **알림함이 세션 전용 50건이라 초반 소식이 밀려난다.** 장기 구동에서 옛 뉴스가
    사라진다 — "저장하지 않는다"는 결정은 유지하되(마이그레이션 짐) 한도·요약을 볼 것
17. **혼잡만으로도 위험 비율이 포화된다.** `riskPoints += crowd × 0.25` 라 스릴 시설이
    0 이어도 붐비면 위험으로 오른다 — K47-③ 범위 밖이라 남겨 뒀다. "실패는 내 선택
    때문이어야"(v4)에 걸리는 축이니 재보정할 것
18. **PSS 갭 제안이 P0 일부 + P1 까지 소화됐다** (`docs/research/pss-gap-proposal.md`).
    K47 이 P0-G 의 가드레일 절반(모달 대신 알림함 적재 = 채널 3분리)을, P1 이
    **A(zone 콤보 면적 비례)** 와 **P1.5-I(시설 개선 분기화)** 를 넣었다. 남은 것:
    **P1-B**(상성 감점 △ — ⚠ PSS 처럼 무효가 다수여야 한다) · **P0-G 연출 본체**(FX 슬롯
    등록부 구현 0건) · **P0-H 목표 3슬롯** · **P2**(사이드 심사 · 손님 선물) ·
    **P3**(메뉴 슬롯 · 완성형 콘텐츠). 권고 순서는 그 문서 §권고 순서에 있다 —
    3번(심사 페이싱)과 뿌리가 같다

⚠ **걷어낸 항목** (이미 돼 있다 — 실측):
- ~~건물 실내 바닥이 잔디로 보인다~~ — `floor_indoor` 가 지면 종류이고 색도 있다 (K27)
- ~~손님 말풍선이 없다~~ — 이모트 아틀라스가 있고 씬이 그린다 (K29)
- ~~`staff.test.ts` 의 안전요원 검사가 공허하게 통과한다~~ — K47-④ 가 손님을 채운 판에서
  재도록 고치고 판정을 `<=` 에서 **`<`** 로 조였다
- ~~위험도 노출이 공원 밖 손님까지 센다~~ — K47-④ 가 `guests.count` → `guests.inside`
  (정원 판정과 같은 값). `risk.test.ts` 픽스처도 `OPEN_GATE_DEFAULTS` 로 바꿨다 —
  그전엔 60명을 뽑아도 전부 공원 밖이라 "붐비면 위험"을 재는 척만 하고 있었다
