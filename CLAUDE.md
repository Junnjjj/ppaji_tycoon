# 빠지 타이쿤

한국 강변 수상레저 "빠지"를 짓고 운영하는 모바일 타이쿤. RollerCoaster Tycoon 의 설계 깊이를
Kairosoft 급 접근성으로, 자유도는 Kairosoft 보다 높게.

**이 프로젝트는 코드와 에셋을 전부 AI 가 만든다.** 여러 세션에 걸쳐 작업하므로,
이 문서가 세션 간의 유일한 기억이다. 아래 결정들을 재유도하거나 뒤집지 말 것.

---

## 아키텍처 불변식 — 절대 깨지 말 것

ESLint 로 강제되고 (`eslint.config.js`), `src/sim/invariants.test.ts` 가
"규칙이 실제로 위반을 잡는지"를 검사한다. 규칙을 끄면 그 테스트가 깨진다. **끄지 말 것.**

### 1. `sim/` 은 렌더러를 모른다

`src/sim/**` 은 `phaser`, `render/`, `ui/`, `assets/`, `save/` 를 import 할 수 없다.
의존 방향은 항상 **바깥 → sim** 이다. sim 은 `toSnapshot()` / `fromSnapshot()` 으로
평문 데이터만 주고받는다.

깨지면: 헤드리스 시뮬 러너가 죽고, 단위 테스트가 불가능해지고, 에셋 교체도 막힌다.

### 2. `sim/` 은 결정론적이다

`Math.random()`, `Date.now()`, `performance.now()`, `new Date()` 금지.
시드 RNG(`src/sim/rng.ts`)와 주입된 tick 만 쓴다.

서브시스템은 `rng.fork(salt)` 로 **독립 스트림**을 갖는다. 손님을 하나 더 뽑아도
날씨 시퀀스가 밀리지 않아야, 밸런싱 실험에서 변수를 하나만 바꿀 수 있다.

깨지면: 헤드리스 밸런싱과 골든 시나리오 회귀 테스트가 전부 무의미해진다.

### 3. 시설·장비·연구는 코드가 아니라 데이터다

`src/data/*.json` 에 정의한다. 시설 추가 = JSON 항목 + 스프라이트 ID.
코드 변경 없음. "시설 30~50개"가 감당 가능해지는 유일한 방법.

---

## 의존성 버전을 올리지 말 것 (이유 있음)

성숙도 기준으로 고정했다. **`npm outdated` 을 보고 "최신으로 올려드릴게요" 하지 말 것.**

| 패키지 | 고정 | 최신이 아닌 이유 |
|---|---|---|
| phaser | **3.90.0** (2025-05) | Phaser 4.0 은 2026-04 출시. Phaser 를 고른 이유 자체가 "문서·예제가 방대해 AI 코드 생성이 안정적"인데, 4.x 는 학습 데이터가 얇아 그 이유를 무너뜨린다 |
| typescript | **~5.9.3** (2025-09) | 7.0.2 는 2026-07 출시·릴리스 1회. 툴체인 정렬이 안 됐다 |
| vite | **^7.3** (2025-06) | 8.x 는 2026-03 |
| vitest | **^3.2** (2025-01) | 4.x 보다 검증됨 |
| eslint | **^9.39** | typescript-eslint 8.x 와 가장 많이 쓰인 조합 |

올릴 거면 이유를 여기 적고 올릴 것.

---

## 구조

```
src/
  sim/        순수 시뮬레이션. Phaser·DOM 비의존. Node 에서 단독 실행됨.
    rng.ts        결정론적 RNG (mulberry32) + fork 로 독립 스트림
    clock.ts      고정 timestep 10Hz. 배속은 tick 수를 곱함(tick 크기가 아님)
    terrain.ts    지형 타입 + 판정 헬퍼
    world.ts      타일맵 (지형만. 시설·손님은 별도 소유)
    worldgen.ts   시드에서 결정론적 맵 생성
    game.ts       시뮬 루트 + 스냅샷
    index.ts      공개 표면 — 바깥은 여기로만 접근
  render/     Phaser 전용
    constants.ts      TILE_SIZE, 줌 범위, LOD 단계
    camera.ts         드래그 팬 + 핀치 줌 (중점 고정 보정 포함)
    terrain-texture.ts 지형 전체를 캔버스 한 장에 구움. 변경 시 영역만 재굽기
    scenes/MainScene.ts
  assets/     에셋 추상화 (§ 아래)
  ui/         DOM/CSS HUD. 캔버스 아님 — 에셋 0장, 선명, 다크모드 공짜
  save/       버전 + 마이그레이션 + 저장소
  data/       시설·장비·연구 정의 JSON (Phase 1~)
tools/
  sim-runner.ts   헤드리스 시뮬레이터
prototype/
  index.html      Phase 0 이전의 렌더링 타당성 검증본. 참고용, 빌드 대상 아님
```

---

## 에셋: "나중에 픽셀로 일괄 교체"

**게임 코드는 파일을 모른다. 논리적 ID 만 안다** (`'facility/shop'`, `'guest/body:3/up/1'`).

- 지금: `ProceduralProvider` — 부팅 시 코드로 스프라이트를 굽는다
- 나중: `AtlasProvider` — AI 픽셀아트 PNG 아틀라스를 로드한다
- 교체 지점: `src/assets/index.ts` 의 `createAssetProvider()` 한 곳뿐. **게임 코드 0줄 변경.**

`src/assets/manifest.json` 이 **이중 역할**을 한다:
1. 코드에게는 계약 — 어떤 ID 가 존재하고 크기·앵커가 무엇인지
2. AI 에셋 생성에게는 작업 지시서 — 무엇을, 몇 px 로, 어떤 스타일로 (`prompt` 필드)

`source` 필드가 파이프라인을 가른다:
- `'procedural'` — **영구히** 코드가 그린다. 물·물결·항적·그림자·지형. AI 생성 대상 아님
- `'ai'` — 지금은 임시 도형, 나중에 AI 픽셀아트로 교체

이 분리 덕에 AI 로 뽑을 이미지가 수백 장이 아니라 **40~50장**이다. 테스트가 이 상한을 지킨다.

---

## 검증

```bash
npm run verify         # typecheck + lint + test  ← 커밋 전 이걸 돌릴 것
npm run sim -- --seed 42 --guests 200 --ticks 20000   # 헤드리스 시뮬 + 성능
npm run sim -- --determinism                          # 결정론 확인
npm run dev            # 폰에서 보려면 출력된 Network URL 사용
npm run verify:mobile  # v1 씬(?v1=1) 모바일 검증 — 폐기된 씬이라 참고용
npm run verify:kairo   # 카이로 씬 브라우저 86종 + 4방 이음새 QA (dev 서버 필요) ← 기본 씬 게이트
npm run seam           # 4방 이음새 QA 단독 (--selftest 로 음성 대조군)
npm run sim:kairo      # 카이로 헤드리스 밸런싱 (--seeds N --weeks N)
npm run verify:pwa     # 빌드 → preview → 오프라인 부팅까지 확인 (홈 화면 앱)
npm run icons          # 앱 아이콘을 코드로 굽는다 (AI 아트 아님 — 포장이다)
```

`npm run sim` 이 도는 것 자체가 불변식 1 의 실증이다 (Phaser 없이 Node 에서 sim 이 돈다).

### 모바일 검증 — 최우선 목표

이 프로젝트의 1순위는 **폰에서 돌아가는 것**이다. "폰에서 확인해주세요"로 넘기지 말고
`npm run verify:mobile` 로 자동 검증할 것. 아이폰 크기(393×852 @3x)·터치를 흉내낸
Chrome 으로 실제 게임을 띄워서 확인한다:

- 부팅·WebGL·콘솔 에러
- 시설 팔레트, **터치 타깃 44px 이상**, 가로 넘침
- 손가락 드래그로 시설 배치 → 확정
- 손님 유입·이동
- FPS
- **핀치 줌** — 반드시 CDP `Input.dispatchTouchEvent` 로 진짜 멀티터치를 보낼 것.
  합성 `PointerEvent` 는 Phaser 가 무시해서 멀쩡한 코드가 실패로 나온다.

브라우저는 번들 chromium 이 아니라 설치된 Chrome 을 쓴다 (`channel: 'chrome'`).

`selftest.html` 은 실기기용 자가진단 페이지다. 폰으로 열면 그 기기의 WebGL·Canvas API·
성능을 재서 판정을 보여준다. 에뮬레이션으로 못 잡는 실기기 문제는 이걸로 확인한다.

**`page.evaluate` 안에서는 이름 있는 함수를 쓰지 말 것.** tsx(esbuild)가 `__name` 헬퍼를
주입하는데 페이지 쪽엔 없어서 ReferenceError 가 난다. 문자열로 넘기는 편이 안전하다.

### 모바일 호환

`src/compat.ts` 가 부팅 전에 먼저 로드된다. 현재 보정:
- `CanvasRenderingContext2D.roundRect` — Safari 16.4 미만에 없다. 없으면 스프라이트를
  굽다가 TypeError 가 나면서 화면이 통째로 검게 남는다.

빌드 타깃은 `['es2020', 'safari15']` — 구형 iOS 까지 내려간다.

---

## 확정된 게임 설계 (v2)

전체 설계는 **`docs/design.md`** — 작업 전에 반드시 읽을 것.

**⚠️ v1(자유 배치 + 실시간 + 자유 스플라인 코스)은 폐기됐다.** 코드에 그 흔적이 남아 있으니
(`PlacementController` 자유 드래그, `CourseEditor` 탭-점찍기, 실시간 배속 UI) v2 로 대체할 것.
왜 바꿨는지는 design.md §1 에 적혀 있다. 되돌리지 말 것.

**한 줄 정의:** 가평 강변 리조트를 키우는 모바일 경영 시뮬. 여름의 "빠지"가 주력, 사계절 운영.

**핵심 루프(30초):** 한 주 진행 → 결산에서 병목·발견 확인 → 구조물을 키우거나 붙임 → 다시 한 주

**바뀐 것과 이유**

| 바뀐 것 | v1 | 최종 | 이유 |
|---|---|---|---|
| 소재 | 빠지 하나 (11종) | **강변 리조트** (시설 62 + 장비 19 = **81종**) | 11종이면 한 시간이면 볼 게 떨어진다 |
| 시간 | 실시간 + 배속 | **주 단위 + 결산 화면** | 실시간은 폰 2분 세션에 안 맞는다. 한 주=0.6초라 공짜로 가능 |
| 배치 | 빈 땅에 툭 놓기 | **연결식 구조물** (데크 확장·부착·건물 증축) | 결정이 거리뿐이라 얇았다. 연결식은 깊고 **폰에서 오히려 쉽다** |
| 코스 | 자유 스플라인 | **프리셋 6종 + 터닝포인트 핸들** | 조각 조합은 탭 10~15회로 폰에 과했다. 프리셋+핸들은 **탭 5회** |
| 코스 깊이 | — | **장비×프리셋 적합도 (19×6)** | 프리셋만으론 30분이면 소진 |
| 수상 제약 | 부력 하중 | **수면 사용 허가 면적**(등급제) | 하중은 계산해보니 안 물었다. 허가는 돈으로 못 산다 |
| 콤보 | 28종 | **70종 3티어** (소40/중20/대10) | 시설 81종에 28종이면 발동이 너무 드물다 |
| 루프 유지 | — | **주간 의사결정 카드 24종** | 없으면 "한 주 진행"이 스킵 버튼이 된다 |
| 시작 | 빈 땅 | **작은 빠지를 물려받음** | 시그니처를 25분→**5분**에 만나게 |

**v4 에서 다시 바뀐 것 — 외부 적대적 평가 반영**

v3 는 "잘 설계된 경영 시뮬"이 됐지만 **"내 빠지를 만드는 재미"가 뒤로 밀렸다**는 지적을 받았다.

| 바뀐 것 | v3 | v4 | 이유 |
|---|---|---|---|
| 주간 진행 | 0.6초 계산 → 결산 | **계산 0.6초 + 압축 연출 3~5초** | "0.6초에 계산된다"→"0.6초만 보여준다"는 논리 비약이었다. 손님이 노는 광경이 이 게임 최대의 보상인데 리플레이로 격리시켰다 |
| 결산 | 숫자 표 | **혼잡 히트맵 + 요일 막대 + 숫자** | 연출을 스킵하면 다시 엑셀 게임이 된다 |
| 경관 | 콤보 조건 일부 | **리조트 5지표 중 하나로 승격** (만족·경관·스릴·안전·청결) | 모든 시설이 `효율 +10%`로 환원돼 공간 창작 동기가 없었다 |
| 콤보 | 무제한 중복 | **중형 30→15→5%, 대형 리조트당 1회** | 안 그러면 최적 콤보 도배가 정답이 된다 |
| 이벤트 | 선택 카드 | **+ 의뢰 16종 (조건 충족형)** | "조건 미달"이 곧 다음 목표가 된다 |
| 날씨 | 방문객 배수 | **수요 구성 변화** (비→카페·사우나↑) | 배수만이면 RNG 세금이다 |
| 사고 | 순수 확률 | **위험도 4단계 상시 표시** | 안전도 78인데 RNG로 폐쇄면 억울하다. 실패는 "내 선택 때문"이어야 |
| 사계절 | 전부 구현 | **MVP는 여름 완성, 비수기 4주씩 압축** | 여름이 재미없으면 사계절도 소용없다 |
| 재플레이 | 시드 생성 | **맵 타입 3종 + 시나리오 6종** | 맵만 다르면 최적 빌드가 같아 2회차 이유가 없다 |
| 최대 축소 | LOD 한 단계 | **감상 화면 + 리조트 이름 + 공유** | 10시간 뒤 리조트가 스크린샷 찍고 싶은 화면이어야 |
| 수익모델 | Phase F로 미룸 | **프리미엄 유료. 광고·IAP 없음** | 광고/IAP는 세션 구조와 진행 속도를 바꾼다. 미루면 루프를 다시 짜야 한다 |

**게임플레이 동사는 6개로 고정** (짓는다·잇는다·코스를 그린다·의뢰를 받는다·값을 매긴다·사람을 쓴다).
재고관리·식자재·직원 스킬트리·대출·세금·물류는 **넣지 않는다.**

**적대적 검증(design.md 전반에 ⚠ 표시)으로 v3 에서 11개, v4 에서 11개를 고쳤다. 되돌리지 말 것.**

**유지되는 것**
- **손님은 개체 단위 에이전트** (통계 아님). 1,200명 = 0.6ms/frame 실측
- **길찾기는 flow field** — 시설별 거리장 1회 계산, 손님당 O(1)
- **플레이어는 지형을 깎지 않는다.** 주어진 지형을 활용 (지형 보너스)
- **평판의 기반은 퇴장 만족도.** "현재 평균 만족도"는 함정 — 할 게 없는 빠지는 손님이 빨리 나가
  새 손님으로 교체되어 평균이 오히려 높다. Phase 1 에서 실제로 걸렸던 부분

## 페이즈

- **Phase 0 뼈대** ✅ — 셋업, 불변식 강제, sim 코어, 에셋 레이어, 세이브, 렌더러, 헤드리스 러너
- **Phase 1 놓고 보는 맛** ✅ — 시설 데이터, 배치·점유 격자, flow field, 손님 에이전트,
  스프라이트 렌더링·LOD, 모바일 배치 UI. `verify:mobile` 전 항목 통과
- **(구 Phase 2 코스)** — sim 은 완성됐으나 **자유 스플라인 방식은 폐기**

### 카이로 전환 (2026-08-18 K0~K9 완료)

전환 기록은 **`docs/kairo-phases.md`** — 작업 전에 읽을 것. 스펙은
`docs/superpowers/specs/2026-08-17-kairo-asset-contract-design.md` (v2, 적대적 리뷰 반영본),
적대적 리뷰 결과는 `docs/kairo-spec-review-2026-08-17.md`.

- **K0 하네스·계약** ✅ · **K1 투영·카메라** ✅ · **K2 지면·경로** ✅ · **K3 벽·도달검사** ✅
- **K4 시설·슬롯** ✅ · **K5 손님** ✅ · **K6 아쿠아파크** ✅ · **K7 주 루프·결산** ✅
- **K8 콤보·해금·의뢰** ✅ · **K9 밸런싱·골든** ✅ · **K10~K24** ✅
- **K25 벽을 경계로 · 건물 영역 · 격자 64×48** ✅

검증: 단위 **624** · 브라우저 **132/132** (`npm run verify:kairo`) · 헤드리스 밸런싱
(`npm run sim:kairo`) · 에셋 게이트 (`npm run gate`)

카이로 씬은 아직 **`?kairo=1`** 뒤에 있다. 기본으로 올리려면 `verify:mobile` 을 옮겨야 한다.

**카이로에서 절대 되돌리지 말 것**
- 카메라 줌은 **영구히 1**. 확대는 `ScaleManager` 정수 배율. Phaser 카메라 줌은 폰 393px 에서
  worldView 를 98.25px 밀어 반 픽셀을 되살린다
- 지면·시설·벽은 **정수 스캔라인**으로 그린다. AA `fill()` 은 1px 이음새와 가짜 아웃라인을 만든다
- 스티플은 **아웃라인을 구운 뒤에** 뚫는다. 순서를 바꾸면 투과율이 0% 가 된다
- 앵커는 (bbox 가로중심, 최하단 꼭지점 y). 최하단 꼭지점의 **x** 를 쓰면 비정사각에서 24텍셀 밀린다
- 시설은 손님 길을 막는다. **플로팅덱·선착장만** 밟고 지나간다
- 벽은 **타일 경계**에 있다 (K25). 칸을 막는 게 아니라 **이동**을 막는다 (`blocksMove`).
  `-I`·`-J` 경계는 이웃이 소유한다 — 한 경계를 두 곳에 저장하면 반드시 어긋난다
- 플레이어는 벽을 한 장씩 놓지 않는다. **영역을 정하면 외곽선이 결과로 생긴다**
  (`BuildingStore`). 맞닿은 두 건물 사이 벽이 사라지는 것은 규칙이 아니라 외곽선의 정의다
- 길찾기의 `canCross` 는 `build()` 와 `next()` **둘 다** 봐야 한다. 하나만 넣으면
  거리장은 맞는데 손님이 벽을 통과한다
- 토지 해금(`landW`/`landH`)은 **봇에도 넣는다**. 안 넣으면 헤드리스 숫자와 실제 판이 갈라진다
- 만족도에는 하향 압력(걷기·대기)이 있어야 한다. 없으면 전 판 만점이 되어 배치가 무의미해진다
- 한 손님의 방문은 하루(120tick) 안에 끝나야 한다. 넘으면 공원이 영구 포화된다
- 결산은 **히트맵 → 요일 막대 → 숫자** 순서. 숫자만이면 다시 엑셀 게임이다
- 위험도는 **상시 표시**. 사고는 경계·위험 단계에서만, 주당 5% 이하

**다음 할 일**: 후반에 지을 게 없다 (막는 것은 자리가 아니라 **등급 상한** — 토지를 넓혀도
그대로다) · 건물 실내 바닥 (지금은 잔디가 그대로 보인다) · 시작 시 물려받는 작은 빠지
(설계 v4 §시작) · 에셋 일괄 생산(Phase G)
